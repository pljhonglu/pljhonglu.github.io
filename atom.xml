<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>独 奏</title>
  <icon>https://www.gravatar.com/avatar/b6fd3124d2934236b914ce8e55a3da13</icon>
  <subtitle>道阻且长，行则将至</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://honglu.me/"/>
  <updated>2018-08-09T03:55:11.827Z</updated>
  <id>https://honglu.me/</id>
  
  <author>
    <name>独 奏</name>
    <email>pljhonglu@gamil.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>负数取模</title>
    <link href="https://honglu.me/2017/05/27/%E8%B4%9F%E6%95%B0%E5%8F%96%E6%A8%A1/"/>
    <id>https://honglu.me/2017/05/27/负数取模/</id>
    <published>2017-05-27T09:00:00.000Z</published>
    <updated>2018-08-09T03:55:11.827Z</updated>
    
    <content type="html"><![CDATA[<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(-7)%10 = ?</span><br></pre></td></tr></table></figure><p>有两个答案：<code>-7</code> 或 <code>3</code></p><p>这就要说到编程语言对于模运算的定义</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">r = a - (a / b) * b</span><br></pre></td></tr></table></figure><p>这其中对于除法运算 <code>a / b</code> 有两种不同的取整方式，一种是向 0 取整，一种是向负无穷取整。这就导致了取模会有两种结果。</p><p>例如：</p><blockquote><p>-7/10 向 0 取整，得到的结果是 0，如果向负无穷取整得到的结果是 -1。因此，如果采用向 0 取整的方式，-7%10 结果就是 -7，如果向负无穷取整，得到的结果就是 3。</p></blockquote><p>到底是向 0 取整还是向负无穷取整，不同的语言实现方式不同，通常默认是按照向 0 取整来计算的，Python 是按照向负无穷取整来计算的，部分语言干脆提供两个函数，rem 和 mod。</p><p>有时候确实需要使用向负无穷取整这种方式怎么办呢。可以通过 <code>floor</code> 函数来计算，该函数是向下取整。那么取模变为如下方式:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">r = a - floor(a / b) * b</span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul><li><a href="http://www.jianshu.com/p/452c1a5acd31" target="_blank" rel="noopener">负数取模怎么算</a></li><li><a href="http://blog.csdn.net/solstice/article/details/5139302" target="_blank" rel="noopener">带符号整数的除法与余数</a></li><li><a href="https://www.zhihu.com/question/34116024" target="_blank" rel="noopener">为什么负数的取余计算各个编程语言结果不统一</a></li><li><a href="http://baike.baidu.com/item/%E5%8F%96%E6%A8%A1%E8%BF%90%E7%AE%97" target="_blank" rel="noopener">取模运算</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;(-7)%10 = ?&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;有两个答案：&lt;code&gt;-7&lt;/code&gt; 或 &lt;code&gt;3&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;这就要说到编程语言对于模运算的定义&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;r = a - (a / b) * b&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;这其中对于除法运算 &lt;code&gt;a / b&lt;/code&gt; 有两种不同的取整方式，一种是向 0 取整，一种是向负无穷取整。这就导致了取模会有两种结果。&lt;/p&gt;
&lt;p&gt;例如：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;-7/10 向 0 取整，得到的结果是 0，如果向负无穷取整得到的结果是 -1。因此，如果采用向 0 取整的方式，-7%10 结果就是 -7，如果向负无穷取整，得到的结果就是 3。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;到底是向 0 取整还是向负无穷取整，不同的语言实现方式不同，通常默认是按照向 0 取整来计算的，Python 是按照向负无穷取整来计算的，部分语言干脆提供两个函数，rem 和 mod。&lt;/p&gt;
&lt;p&gt;有时候确实需要使用向负无穷取整这种方式怎么办呢。可以通过 &lt;code&gt;floor&lt;/code&gt; 函数来计算，该函数是向下取整。那么取模变为如下方式:&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;r = a - floor(a / b) * b&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="有意思" scheme="https://honglu.me/categories/%E6%9C%89%E6%84%8F%E6%80%9D/"/>
    
    
      <category term="模" scheme="https://honglu.me/tags/%E6%A8%A1/"/>
    
  </entry>
  
  <entry>
    <title>深入理解 RunLoop</title>
    <link href="https://honglu.me/2017/03/30/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3RunLoop/"/>
    <id>https://honglu.me/2017/03/30/深入理解RunLoop/</id>
    <published>2017-03-30T02:00:00.000Z</published>
    <updated>2019-06-12T06:17:51.234Z</updated>
    
    <content type="html"><![CDATA[<h2 id="事件驱动模型"><a href="#事件驱动模型" class="headerlink" title="事件驱动模型"></a>事件驱动模型</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while (AppIsRunning) &#123;</span><br><span class="line">    id whoWakesMe = SleepForWakingUp();</span><br><span class="line">    id event = GetEvent(whoWakesMe);</span><br><span class="line">    HandleEvent(event);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>大部分的 GUI 程序都使用了事件驱动模型，有些服务器(nginx, node.js)也使用了事件驱动模型。相比于轮询等其他方式，其优点在于极大的提高了 CPU 使用率，在没有事件的时候，能够让出 CPU 时间片，来事件时也可以快速的得到响应。</p><a id="more"></a><h2 id="Cocoa-中的-RunLoop"><a href="#Cocoa-中的-RunLoop" class="headerlink" title="Cocoa 中的 RunLoop"></a>Cocoa 中的 RunLoop</h2><p>我们常用到的是 NSRunLoop ，其位于 Foundation 框架中。Foundation 框架实际上是 <code>Core Foundation</code> 的部分导出。而 <code>Core Foundation</code> 的源码是开源的。NSRunLoop 实际上是 CFRunLoop 的高层抽象。CFRunLoop 源码可以从<a href="https://opensource.apple.com/source/CF/" target="_blank" rel="noopener">这里</a> 下载到。</p><p>RunLoop 在 Cocoa 中的应用相当广泛。</p><p>NSTimer，UIEvent，NSObject (NSDelayedPerforming)，NSObject (NSThreadPerformAdditions)，NSURLConnection 是通过 Source 的机制把事件加入到 RunLoop 中。</p><p>Autorelease，CADisplayLink，CATransition，CAAnimation 则是利用 observer 的机制，在 RunLoop 的循环周期中执行各自的操作。</p><p>更加底层的 GCD，mach kernel 也与 RunLoop 存在协作关系。</p><p>使用 Xcode 调试的时候我们经常可以看到 RunLoop 相关的堆栈信息：</p><p><img src="http://blog.qiniu.lololo.store/2017-03-31-runloop断点.jpg" alt="runloop断点"></p><p>图中可以看到四处关于 RunLoop 的堆栈信息，最上面一条 <code>__CFRUNLOOP_IS_CALLING_OUT_TO_AN_OBSERVER_CALLBACK_FUNCTION__</code> 则是 RunLoop 中的 observer 的 call out 调用。其它的调用还包括：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">__CFRUNLOOP_IS_CALLING_OUT_TO_AN_OBSERVER_CALLBACK_FUNCTION__</span><br><span class="line">__CFRUNLOOP_IS_CALLING_OUT_TO_A_TIMER_CALLBACK_FUNCTION__</span><br><span class="line">__CFRUNLOOP_IS_CALLING_OUT_TO_A_SOURCE0_PERFORM_FUNCTION__</span><br><span class="line">__CFRUNLOOP_IS_CALLING_OUT_TO_A_SOURCE1_PERFORM_FUNCTION__</span><br><span class="line">__CFRUNLOOP_IS_SERVICING_THE_MAIN_DISPATCH_QUEUE__</span><br><span class="line">__CFRUNLOOP_IS_CALLING_OUT_TO_A_BLOCK__</span><br></pre></td></tr></table></figure><p>图中还可以看出 CATransition 利用了 RunLoop 的 observer 做视图的相关操作。</p><h2 id="RunLoop-机制"><a href="#RunLoop-机制" class="headerlink" title="RunLoop 机制"></a>RunLoop 机制</h2><p><img src="http://blog.qiniu.lololo.store/2017-03-31-RunLoop结构.png" alt="RunLoop结构"></p><p>这一张图我们可以看出 RunLoop 的组织结构关系。</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">RunLoop : thread = 1:1</span><br><span class="line">RunLoop : RunLoopMode = 1:n</span><br><span class="line">RunLoopMode : CFRunLoop* = 1:n</span><br></pre></td></tr></table></figure><p>下面针对各个部分进行了说明</p><h3 id="CFRunLoop"><a href="#CFRunLoop" class="headerlink" title="CFRunLoop"></a>CFRunLoop</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> __CFRunLoop * <span class="built_in">CFRunLoopRef</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> __CFRunLoop &#123;</span><br><span class="line">    <span class="built_in">CFRuntimeBase</span> _base;</span><br><span class="line">    pthread_mutex_t _lock;<span class="comment">/* locked for accessing mode list */</span></span><br><span class="line">    __CFPort _wakeUpPort;<span class="comment">// used for CFRunLoopWakeUp </span></span><br><span class="line">    Boolean _unused;</span><br><span class="line">    <span class="keyword">volatile</span> _per_run_data *_perRunData;              <span class="comment">// reset for runs of the run loop</span></span><br><span class="line">    pthread_t _pthread;</span><br><span class="line">    uint32_t _winthread;</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _commonModes; <span class="comment">// 字符串，记录所有标记为common的mode</span></span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _commonModeItems; <span class="comment">// 所有commonMode的item(source、timer、observer)</span></span><br><span class="line">    <span class="built_in">CFRunLoopModeRef</span> _currentMode;</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _modes; <span class="comment">// CFRunLoopModeRef set</span></span><br><span class="line">    <span class="keyword">struct</span> _block_item *_blocks_head;</span><br><span class="line">    <span class="keyword">struct</span> _block_item *_blocks_tail;</span><br><span class="line">    <span class="built_in">CFTypeRef</span> _counterpart;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ul><li>CFRunLoop 里面包含了线程，若干个 mode。</li><li>CFRunLoop 和线程是一一对应的。</li><li>_blocks_head 是 perform block 加入到里面的</li></ul><h3 id="CFRunLoopMode"><a href="#CFRunLoopMode" class="headerlink" title="CFRunLoopMode"></a>CFRunLoopMode</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义 CFRunLoopModeRef 为指向 __CFRunLoopMode 结构体的指针</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> __CFRunLoopMode *<span class="built_in">CFRunLoopModeRef</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> __CFRunLoopMode &#123;</span><br><span class="line">    <span class="built_in">CFRuntimeBase</span> _base;</span><br><span class="line">    pthread_mutex_t _lock;<span class="comment">/* must have the run loop locked before locking this */</span></span><br><span class="line">    <span class="built_in">CFStringRef</span> _name;</span><br><span class="line">    Boolean _stopped;</span><br><span class="line">    <span class="keyword">char</span> _padding[<span class="number">3</span>];</span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _sources0; <span class="comment">// source0 set ，非基于Port的，接收点击事件，触摸事件等APP 内部事件</span></span><br><span class="line">    <span class="built_in">CFMutableSetRef</span> _sources1; <span class="comment">// source1 set，基于Port的，通过内核和其他线程通信，接收，分发系统事件</span></span><br><span class="line">    <span class="built_in">CFMutableArrayRef</span> _observers; <span class="comment">// observer 数组</span></span><br><span class="line">    <span class="built_in">CFMutableArrayRef</span> _timers; <span class="comment">// timer 数组</span></span><br><span class="line">    <span class="built_in">CFMutableDictionaryRef</span> _portToV1SourceMap;<span class="comment">// source1 对应的端口号</span></span><br><span class="line">    __CFPortSet _portSet;</span><br><span class="line">    <span class="built_in">CFIndex</span> _observerMask;</span><br><span class="line"><span class="meta">#if USE_DISPATCH_SOURCE_FOR_TIMERS</span></span><br><span class="line">    dispatch_source_t _timerSource;</span><br><span class="line">    <span class="built_in">dispatch_queue_t</span> _queue;</span><br><span class="line">    Boolean _timerFired; <span class="comment">// set to true by the source when a timer has fired</span></span><br><span class="line">    Boolean _dispatchTimerArmed;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line"><span class="meta">#if USE_MK_TIMER_TOO</span></span><br><span class="line">    mach_port_t _timerPort;</span><br><span class="line">    Boolean _mkTimerArmed;</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line"><span class="meta">#if DEPLOYMENT_TARGET_WINDOWS</span></span><br><span class="line">    DWORD _msgQMask;</span><br><span class="line">    <span class="keyword">void</span> (*_msgPump)(<span class="keyword">void</span>);</span><br><span class="line"><span class="meta">#endif</span></span><br><span class="line">    uint64_t _timerSoftDeadline; <span class="comment">/* TSR */</span></span><br><span class="line">    uint64_t _timerHardDeadline; <span class="comment">/* TSR */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ul><li>runloop mode 里面包含了source0、source1、timer、observer 各种事件，以及 port 集合。其负责管理这些事件。</li></ul><p>CFRunLoopMode 包含下面几种 mode</p><table><thead><tr><th>mode</th><th>name</th><th>description</th></tr></thead><tbody><tr><td>Default</td><td>NSDefaultRunLoopMode (Cocoa) kCFRunLoopDefaultMode (Core Foundation)</td><td>用的最多的模式，大多数情况下应该使用该模式开始 RunLoop并配置 input source</td></tr><tr><td>Connection</td><td>NSConnectionReplyMode (Cocoa)</td><td>Cocoa用这个模式结合 NSConnection 对象监测回应，我们应该很少使用这种模式</td></tr><tr><td>Modal</td><td>NSModalPanelRunLoopMode (Cocoa)</td><td>Cocoa用此模式来标识用于模态面板的事件</td></tr><tr><td>Event tracking</td><td>NSEventTrackingRunLoopMode (Cocoa)</td><td>Cocoa使用此模式在鼠标拖动loop和其它用户界面跟踪 loop期间限制传入事件</td></tr><tr><td>Common modes</td><td>NSRunLoopCommonModes (Cocoa) kCFRunLoopCommonModes (Core Foundation)</td><td>这是一组可配置的常用模式。将输入源与些模式相关联会与组中的每个模式相关联。Cocoa applications 里面此集包括Default、Modal和Event tracking。Core Foundation只包括默认模式，你可以自己把自定义mode用CFRunLoopAddCommonMode函数加入到集合中.</td></tr></tbody></table><h3 id="RunLoopSource"><a href="#RunLoopSource" class="headerlink" title="RunLoopSource"></a>RunLoopSource</h3><p>RunLoopSource 分为 source0 和 source1。</p><ul><li>source0 是非基于 port 的事件，主要是 APP 内部事件，如点击事件，触摸事件等。</li><li>source1 是基于Port的，通过内核和其他线程通信，接收，分发系统事件。</li><li><strong>CFRunLoopSource 里面包含一个 _runLoops，也就意味着一个 </strong>CFRunLoopSource 可以被添加到多个 runloop mode 中去。</li></ul><h3 id="RunLoopObserver"><a href="#RunLoopObserver" class="headerlink" title="RunLoopObserver"></a>RunLoopObserver</h3><p>CFRunLoopObserver 是观察者，可以观察RunLoop的各种状态，并抛出回调。可以监听得状态如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/* Run Loop Observer Activities */</span><br><span class="line">typedef CF_OPTIONS(CFOptionFlags, CFRunLoopActivity) &#123;</span><br><span class="line">    kCFRunLoopEntry = (1UL &lt;&lt; 0), //即将进入run loop</span><br><span class="line">    kCFRunLoopBeforeTimers = (1UL &lt;&lt; 1), //即将处理timer</span><br><span class="line">    kCFRunLoopBeforeSources = (1UL &lt;&lt; 2), //即将处理source</span><br><span class="line">    kCFRunLoopBeforeWaiting = (1UL &lt;&lt; 5), //即将进入休眠</span><br><span class="line">    kCFRunLoopAfterWaiting = (1UL &lt;&lt; 6), //被唤醒但是还没开始处理事件</span><br><span class="line">    kCFRunLoopExit = (1UL &lt;&lt; 7), //run loop已经退出</span><br><span class="line">    kCFRunLoopAllActivities = 0x0FFFFFFFU</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="RunLoopTimer"><a href="#RunLoopTimer" class="headerlink" title="RunLoopTimer"></a>RunLoopTimer</h3><ul><li>CFRunLoopTimer 是定时器，可以在设定的时间点抛出回调</li><li>CFRunLoopTimer和NSTimer是toll-free bridged的，可以相互转换。</li></ul><h2 id="CFRunLoopGet"><a href="#CFRunLoopGet" class="headerlink" title="CFRunLoopGet"></a>CFRunLoopGet</h2><ul><li>RunLoop 和线程是一一对应的</li><li>不允许直接创建RunLoop</li><li>线程刚创建时并没有 RunLoop, 如果你不主动获取, 那它一直都不会有</li><li>你只能在一个线程的内部获取其 RunLoop(main thread 除外)</li><li>RunLoop 会在线程销毁时销毁</li><li>RunLoop 和线程的一一对应关系保存一个全局 Dictionary 里 __CFRunLoops</li><li>即使主线程，不主动创建也不会有 RunLoop，只是 application 会在主线程默认创建 RunLoop</li><li>如果创建子线程 RunLoop，主线程会默认加上一个 RunLoop</li></ul><h2 id="CFRunLoopRun"><a href="#CFRunLoopRun" class="headerlink" title="CFRunLoopRun"></a>CFRunLoopRun</h2><p><img src="http://blog.qiniu.lololo.store/2017-03-31-RunLoopRun.png" alt="RunLoopRun"></p><p>执行过程大致描述如下：</p><ol><li>通知 observers 即将进入 run loop</li><li>通知 observes 即将开始处理 timer source</li><li>通知 observes 即将开始处理 source0 事件</li><li>执行 source0 事件</li><li>如果处于主线程有 dispatchPort 消息，跳转到第9步</li><li>通知 observes 线程即将进入休眠</li><li>内循环阻塞等待接收系统消息，包括：<ul><li>收到内核发送过来的消息 （source1消息）</li><li>定时器事件需要执行</li><li>run loop 的超时时间到了</li><li>手动唤醒 run loop</li></ul></li><li>通知 observes 线程被唤醒</li><li>处理通过端口收到的消息：<ul><li>如果自定义的 timer 被 fire，那么执行该 timer 事件并重新开始循环，完成后跳转到第2步</li><li>如果 input source 被 fire，则处理该事件</li><li>如果 run loop 被手动唤醒，并且没有超时，完成后跳转到第2步</li></ul></li><li>通知 observes run loop 已经退出</li></ol><p>注意：</p><ul><li>CFRunLoopDoBlocks 是执行 perform block 中的 block</li><li>绿色的是RunLoopRun()</li><li>第一次循环 CFRunLoopServiceMachPort 是不走的</li><li>handle_msg 处理 timer 事件，处理 main queue block 事件，处理 source1 事件</li><li>中间的红色<code>CFRunLoopServiceMachPort</code>是监听  GCD 的端口事件，只监听一个端口，左下角的<code>CFRunLoopServiceMachPort</code>是坚挺 source1,timer 的，是一个 MutableSet</li></ul><h2 id="RunLoop-与-GCD"><a href="#RunLoop-与-GCD" class="headerlink" title="RunLoop 与 GCD"></a>RunLoop 与 GCD</h2><p>RunLoop 与 GCD 是互相协作的关系，RunLoop 的最开始部分使用了 GCD 的 timer 做超时的回调；通过 GCD 调用带有 RunLoop 的线程的 block，会通过 <code>dispatch port CFRunLoopServiceMachPort</code> 把事件发送到该线程的 RunLoop 里面。</p><p>比如：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">dispatch_async(dispatch_get_main_queue(), ^&#123;&#125;);</span><br></pre></td></tr></table></figure><p>主线程存在 runloop，那么 GCD 会通过 <code>dispatch port CFRunLoopServiceMachPort</code>，把事件发送给 RunLoop，RunLoop 接收到时间之后，会执行这个 block。</p><h3 id="NSTimer-与-GCD-Timer"><a href="#NSTimer-与-GCD-Timer" class="headerlink" title="NSTimer 与 GCD Timer"></a>NSTimer 与 GCD Timer</h3><p>NSTimer 是通过 RunLoop 的 RunLoopTimer 把时间加入到 RunLoopMode 里面。官方文档里面也有说 CFRunLoopTimer 和 NSTimer 是可以互相转换的。由于 NSTimer 的这种机制，因此 NSTimer 的执行必须依赖于 RunLoop，如果没有 RunLoop，NSTimer 是不会执行的。</p><p>GCD 则不同，GCD 的线程管理是通过系统来直接管理的。GCD Timer 是通过 dispatch port 给 RunLoop 发送消息，来使 RunLoop 执行相应的 block，如果所在线程没有 RunLoop，那么 GCD 会临时创建一个线程去执行 block，执行完之后再销毁掉，因此 GCD 的 Timer 是不依赖 RunLoop 的。</p><p>至于这两个 Timer 的准确性问题，如果不再 RunLoop 的线程里面执行，那么只能使用 GCD Timer，由于 GCD Timer 是基于 MKTimer(mach kernel timer)，已经很底层了，因此是很准确的。</p><p>如果在 RunLoop 的线程里面执行，由于 GCD Timer 和 NSTimer 都是通过 port 发送消息的机制来触发 RunLoop 的，因此准确性差别应该不是很大。如果线程 RunLoop 阻塞了，不管是 GCD Timer 还是 NSTimer 都会存在延迟问题。</p><h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><ul><li>异步的回调如果存在延时操作，那么就要放到有 RunLoop 的线程里面，否则回调没有着陆点无法执行</li><li>NSTimer 必须得在有 RunLoop 的线程里面才能执行，另外，使用 NSTimer 的时候会出现滑动 TableView，Timer 停止的问题，是由于 RunLoopMode 切换的问题，只要把 NSTimer 加到 common mode 就好了。</li><li>滚动过程中延迟加载，可以利用滚动时 RunLoopMode 切换到 NSEventTrackingRunLoopMode 模式下这个机制，在 Default mode 下添加加载图片的方法，在滚动时就不会触发。</li><li>崩溃后处理 <a href="https://github.com/walkdianzi/DSSignalHandlerDemo" target="_blank" rel="noopener">DSSignalHandlerDemo</a></li></ul><h2 id="演示代码"><a href="#演示代码" class="headerlink" title="演示代码"></a>演示代码</h2><p>代码中演示了 RunLoop 的不同事件机制，也演示了 GCD Timer 和 NStimer 的区别。</p><p>Demo: <a href="http://blog.qiniu.lololo.store/RunLoopTest.zip" target="_blank" rel="noopener">RunLoopTest</a></p><p>源码注释: <a href="http://blog.qiniu.lololo.store/source.zip" target="_blank" rel="noopener">source</a></p><p>PPT: <a href="http://blog.qiniu.lololo.store/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%20RunLoop.key" target="_blank" rel="noopener">深入理解 RunLoop</a></p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="http://blog.ibireme.com/2015/05/18/runloop/" target="_blank" rel="noopener">深入理解RunLoop</a><br><a href="http://aaaboom.com/?p=34#wow24" target="_blank" rel="noopener">RunLoop系列之源码分析</a><br><a href="http://aaaboom.com/?p=37" target="_blank" rel="noopener">RunLoop系列之要点提炼</a>  </p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;事件驱动模型&quot;&gt;&lt;a href=&quot;#事件驱动模型&quot; class=&quot;headerlink&quot; title=&quot;事件驱动模型&quot;&gt;&lt;/a&gt;事件驱动模型&lt;/h2&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;while (AppIsRunning) &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    id whoWakesMe = SleepForWakingUp();&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    id event = GetEvent(whoWakesMe);&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    HandleEvent(event);&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;大部分的 GUI 程序都使用了事件驱动模型，有些服务器(nginx, node.js)也使用了事件驱动模型。相比于轮询等其他方式，其优点在于极大的提高了 CPU 使用率，在没有事件的时候，能够让出 CPU 时间片，来事件时也可以快速的得到响应。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="iOS" scheme="https://honglu.me/tags/iOS/"/>
    
      <category term="RunLoop" scheme="https://honglu.me/tags/RunLoop/"/>
    
  </entry>
  
  <entry>
    <title>计算机浮点数那些事</title>
    <link href="https://honglu.me/2017/03/12/%E8%AE%A1%E7%AE%97%E6%9C%BA%E6%B5%AE%E7%82%B9%E6%95%B0%E9%82%A3%E4%BA%9B%E4%BA%8B/"/>
    <id>https://honglu.me/2017/03/12/计算机浮点数那些事/</id>
    <published>2017-03-12T02:13:00.000Z</published>
    <updated>2019-06-12T06:17:51.236Z</updated>
    
    <content type="html"><![CDATA[<h1 id="十进制小数转二进制小数"><a href="#十进制小数转二进制小数" class="headerlink" title="十进制小数转二进制小数"></a>十进制小数转二进制小数</h1><p>对十进制小数乘2得到的整数部分和小数部分，整数部分既是相应的二进制数码；<br>再用2乘小数部分(之前乘后得到新的小数部分)，又得到整数和小数部分；<br>如此不断重复，直到小数部分为0或达到精度要求为止。<br>第一次所得到为最高位，最后一次得到为最低位。</p><a id="more"></a><p>如:</p><p>0.8125的二进制：</p><p>0.8125<em>2=1.625   取整是1<br>0.625</em>2=1.25     取整是1<br>0.25<em>2=0.5       取整是0<br>0.5</em>2=1.0        取整是1</p><p>即0.8125的二进制是0.1101（第一次所得到为最高位,最后一次得到为最低位）</p><p>0.1 的二进制：</p><table><thead><tr><th>乘数</th><th>乘2结果</th><th>整数部分</th><th>说明</th></tr></thead><tbody><tr><td>0.1</td><td>0.2</td><td>0</td><td></td></tr><tr><td>0.2</td><td>0.4</td><td>0</td><td></td></tr><tr><td>0.4</td><td>0.8</td><td>0</td><td></td></tr><tr><td>0.8</td><td>1.6</td><td>1</td><td>减1</td></tr><tr><td>0.6</td><td>1.2</td><td>1</td><td>减1</td></tr><tr><td>0.2</td><td>0.4</td><td>0</td><td></td></tr><tr><td>0.4</td><td>0.8</td><td>0</td><td></td></tr><tr><td>0.8</td><td>1.6</td><td>1</td><td>减1</td></tr><tr><td>0.6</td><td>1.2</td><td>1</td><td>减1</td></tr><tr><td>…</td><td>…</td><td>…</td><td></td></tr></tbody></table><p>可见，十进制 0.1 转为二进制后为 <code>0.000110011001100110011...</code> 无限循环小数</p><h1 id="计算机浮点数表示"><a href="#计算机浮点数表示" class="headerlink" title="计算机浮点数表示"></a>计算机浮点数表示</h1><p>计算机存储浮点数遵循 <a href="http://www.ibm.com/developerworks/cn/java/j-jtp0114/index.html" target="_blank" rel="noopener">IEEE 754</a> 格式。</p><blockquote><p>V = (-1)^S <em> M </em> 2^E</p></blockquote><ul><li>S(Sign/1bit)：符号部分，当 S=0，V 为正数；当 S=1，V 为负数。</li><li>E(Exponent/8bits)：指数部分。以2为底。需要注意的是，E 在这里是一个无符号整数，但是实际上指数是可以存在负数的，所以 <a href="http://www.ibm.com/developerworks/cn/java/j-jtp0114/index.html" target="_blank" rel="noopener">IEEE 754</a> 规定，E 需要偏移127(64位为1023)，如，2^10 的指数是10，保存成32位浮点数时，必须保存成 10+127=137，即10001001。</li><li>M(Mantissa/23bits)：表示有效数字，大于等于1，小于2。第一位 1 可以被舍去，只保留小数部分节省一位有效数字。</li></ul><p>指数 E 还分为三种情况：</p><ol><li>E 不全为0或不全为1。这时，浮点数就采用上面的规则表示，即指数E的计算值减去127（或1023），得到真实值，再将有效数字M前加上第一位的1。</li><li>E全为0。这时，浮点数的指数E等于 1-127（或者 1-1023），有效数字M不再加上第一位的1，而是还原为 0.xxxxxx 的小数。这样做是为了表示±0，以及接近于0的很小的数字。</li><li>E全为1。这时，如果有效数字M全为0，表示±无穷大（正负取决于符号位s）；如果有效数字M不全为0，表示这个数不是一个数（NaN）。</li></ol><p><a href="http://www.ibm.com/developerworks/cn/java/j-jtp0114/index.html" target="_blank" rel="noopener">IEEE 754</a> 中规定：</p><p>对于32位的浮点数，最高的1位是符号位S，接着的8位是指数E，剩下的23位为有效数字M；<br>对于64位的浮点数，最高的1位是符号位S，接着的11位是指数E，剩下的52位为有效数字M。  </p><p><img src="http://blog.qiniu.lololo.store/2016-08-02-14701202537550.gif" alt=""></p><h2 id="计算误差"><a href="#计算误差" class="headerlink" title="计算误差"></a>计算误差</h2><p>下面以双精度 double 类型的 0.1+0.2 为例</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">十进制 0.1</span><br><span class="line">=&gt; 二进制 0.00011001100110011...(循环0011)</span><br><span class="line">=&gt; 尾数为 1.1001100110011001100...1100（共52位，除了小数点左边的1），指数为 -4（偏移1023后二进制为 01111111011）,符号位为0</span><br><span class="line">=&gt; 存储为：0 01111111011 1001100110011001100110011001100110011001100110011001</span><br><span class="line">=&gt; 因为尾数最多52位，所以实际存储的值为0.00011001 10011001 10011001 10011001 10011001 10011001 10011001</span><br><span class="line"></span><br><span class="line">十进制0.2</span><br><span class="line">=&gt; 二进制0.0011001100110011...(循环0011)</span><br><span class="line">=&gt; 尾数为 1.1001100110011001100...1100（共52位，除了小数点左边的1），指数为-3（偏移1023后二进制为 1111111100）,符号位为0</span><br><span class="line">=&gt; 存储为：0 01111111100 1001100110011001100110011001100110011001100110011001</span><br><span class="line">因为尾数最多52位，所以实际存储的值为0.00110011 00110011 00110011 00110011 00110011 00110011 00110011</span><br><span class="line"></span><br><span class="line">两者相加</span><br><span class="line"> 0.00011001 10011001 10011001 10011001 10011001 10011001 10011001</span><br><span class="line">+0.00110011 00110011 00110011 00110011 00110011 00110011 00110011</span><br><span class="line">=0.01001100 11001100 11001100 11001100 11001100 11001100 11001100</span><br><span class="line">转换成10进制之后得到：0.30000000000000004</span><br></pre></td></tr></table></figure><p>可以发现，由于十进制下的部分小数无法使用二进制精确表示，所以会存在一定的计算误差。在平常应用中我们很少会遇到这个问题，比如遇到支付宝这样的货币系统，需要高精度的计算时，通常我们会定义精确到小数点后两位，然后使用整形来代替浮点型，即使用一分钱的整数倍。</p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;十进制小数转二进制小数&quot;&gt;&lt;a href=&quot;#十进制小数转二进制小数&quot; class=&quot;headerlink&quot; title=&quot;十进制小数转二进制小数&quot;&gt;&lt;/a&gt;十进制小数转二进制小数&lt;/h1&gt;&lt;p&gt;对十进制小数乘2得到的整数部分和小数部分，整数部分既是相应的二进制数码；&lt;br&gt;再用2乘小数部分(之前乘后得到新的小数部分)，又得到整数和小数部分；&lt;br&gt;如此不断重复，直到小数部分为0或达到精度要求为止。&lt;br&gt;第一次所得到为最高位，最后一次得到为最低位。&lt;/p&gt;
    
    </summary>
    
      <category term="有意思" scheme="https://honglu.me/categories/%E6%9C%89%E6%84%8F%E6%80%9D/"/>
    
    
      <category term="浮点" scheme="https://honglu.me/tags/%E6%B5%AE%E7%82%B9/"/>
    
  </entry>
  
  <entry>
    <title>HTTPS 总结</title>
    <link href="https://honglu.me/2017/01/01/HTTPS%E6%80%BB%E7%BB%93/"/>
    <id>https://honglu.me/2017/01/01/HTTPS总结/</id>
    <published>2017-01-01T02:00:00.000Z</published>
    <updated>2019-06-12T06:17:51.234Z</updated>
    
    <content type="html"><![CDATA[<p>这是内部分享的资料，也是对前面几篇关于 HTTPS 相关文章的一个总结。现公开出来。前面的两篇关于 HTTPS 的文章链接如下：</p><ul><li><a href="http://honglu.me/2016/01/13/HTTPS%E8%AF%A6%E8%A7%A3/">HTTPS 详解</a>  </li><li><a href="http://honglu.me/2016/10/03/HTTPS%E8%AF%A6%E8%A7%A3(%E4%BA%8C)/">HTTPS 详解(二)</a></li></ul><p>下面是我使用 wireshark 进行抓包的文件：</p><p><a href="http://blog.qiniu.lololo.store/package.pcapng" target="_blank" rel="noopener">package</a></p><p>可以下载下来使用 wireshark 打开后对比来看。</p><a id="more"></a><h2 id="HTTPS-TLS-层握手流程"><a href="#HTTPS-TLS-层握手流程" class="headerlink" title="HTTPS TLS 层握手流程"></a>HTTPS TLS 层握手流程</h2><p><img src="http://blog.qiniu.lololo.store/2017-02-09-14832472425287.png" alt=""></p><ol><li>客户端握手请求(Client Hello)：<ul><li>支持的协议版本。</li><li>客户端随机数(<code>random_1</code>)，这个随机数既需要客户端保存又需要发送给服务器。</li><li>支持的加密方法。</li><li>支持的压缩方法。</li></ul></li><li><p>服务器回复：</p><p> <strong>server hello</strong></p><ul><li>确认使用的加密通信协议版本。</li><li>服务器随机数(<code>random_2</code>)。</li><li><p>确认使用的加密方法。</p><p><strong>certificate</strong></p><p>发送服务器证书。</p><p><strong>server key exchange</strong></p><p>当服务端向客户端发送的证书中没有提供足够的信息的时候，还可以向客户端发送一个 Server Key Exchange。如使用 RSA 算法，则服务端可以在这里提供加密预主密钥(PreMaster Secret)的公钥。如使用 DH 算法，则这里传输 DH parameter。</p><p><strong>certificate request</strong></p><p>若服务端需验证客户端证书，需要发送客户端证书请求。</p><p><strong>server hello done</strong></p></li></ul></li><li><p>客户端回应：</p><p> 验证服务器证书的合法性。</p><p> <strong>certificate</strong></p><p> 如果前一步服务端需要客户端提供证书，则通过这里发送客户端证书</p><p> <strong>client key exchange</strong></p><p> 若使用 RSA 算法生成密钥对，则这里生成 <code>PreMaster Secret</code> 并用前面服务器发送过来的公钥加密。若使用 DH 算法，则这里传输 DH parameter</p><p> <strong>certificate vertify</strong></p><p>  只有在客户端发送了自己证书到服务器端，这个消息才需要发送。该消息是让服务端验证该客户端证书，证明客户端确实有该证书的私钥。</p><p> <strong>change cipher spec</strong></p><p> 编码改变通知，表示随后的信息都将用双方商定的加密方法和密钥发送。</p><p> <strong>encrypted handshake message</strong></p><p> 客户端向服务器端发送一个加密的报文(前面所有发送内容的摘要)，如果服务器能够解密，并验证成功，说明两端密钥一致</p></li><li><p>服务器回应</p><p><strong>change cipher spec</strong></p><p>编码改变通知，表示随后的信息都将用双方商定的加密方法和密钥发送。</p><p><strong>encrypted handshake message</strong></p><p>服务器向客户端发送一个加密的报文(前面所有发送内容的摘要)，如果客户端能够解密，并验证成功，说明两端密钥一致。</p></li><li><p>application data</p><p>传输使用公共密钥加密的数据</p></li></ol><h2 id="读懂加密方式"><a href="#读懂加密方式" class="headerlink" title="读懂加密方式"></a>读懂加密方式</h2><p><code>TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384</code></p><p>ECDHE_RSA: 协商密钥时使用的算法，ECDHE 用于密钥交换，RSA 用于身份认证。<br>AES_256_GCM: 数据交换时使用的加密算法<br>SHA384: 签名算法</p><h2 id="链接重用"><a href="#链接重用" class="headerlink" title="链接重用"></a>链接重用</h2><p>TLS 握手阶段需要9次请求，非常耗时。链接重用目前有两种方式</p><h3 id="session-ID"><a href="#session-ID" class="headerlink" title="session ID"></a>session ID</h3><p>session ID的思想很简单，就是每一次对话都有一个编号（session ID）。如果对话中断，下次重连的时候，只要客户端给出这个编号，且服务器有这个编号的记录，双方就可以重新使用已有的”对话密钥”，而不必重新生成。</p><p>该方式类似服务端的 session，可以使用 session ID 作为该次回话的唯一 标识，缺点在于 session id 需要服务器做存储，对于服务器集群来说，需要专门维护 session ID，可能造成数据不同步的问题。</p><h3 id="session-ticket"><a href="#session-ticket" class="headerlink" title="session ticket"></a>session ticket</h3><p>这个session ticket是加密的，只有服务器才能解密，其中包括本次对话的主要信息，比如对话密钥和加密方法等。当服务器收到session ticket以后，可直接从中解密出对话密钥。</p><p>这种方式的优点就在于不需要服务器存储。缺点在于可携带的信息量有限。</p><h2 id="CDN"><a href="#CDN" class="headerlink" title="CDN"></a>CDN</h2><p>网站为了加快访问速度通常会使用 CDN，如果使用外部 CDN，我们通常不会把私钥交给 CDN 服务商，针对这种场景 CloudFlare 公司提出了一种解决方案 —— <a href="https://www.cloudflare.com/ssl/keyless-ssl/" target="_blank" rel="noopener">Keyless SSL</a>。</p><p><img src="http://blog.qiniu.lololo.store/2016-09-23-14746019657998.png" alt=""></p><p>上图中主要区别在于第四步，CDN 提供商拿到前一步由源服务器公钥加密过的密文后，通过安全的信道交给源服务器来解密，由源服务器提取会话秘钥或完成签名以后再提交给 CDN 节点。如此 CDN 就获得了此次回话的密钥。</p><h2 id="Diffie-Hellman-算法"><a href="#Diffie-Hellman-算法" class="headerlink" title="Diffie-Hellman 算法"></a>Diffie-Hellman 算法</h2><p>由于握手交换随机数的过程是不加密的，因此，如果有人窃听通信，他可以知道双方选择的加密方法，以及三个随机数中的两个。整个通话的安全，只取决于第三个随机数（Premaster secret）能不能被破解。</p><p>虽然理论上，只要服务器的公钥足够长（比如2048位），那么Premaster secret可以保证不被破解。但是为了足够安全，我们可以把协商密钥阶段通过 RSA 传输密钥，改为使用 Diffie-Hellman 算法（简称DH算法）生成密钥。</p><p>采用DH算法后，Premaster secret不需要传递，双方只要交换各自的参数，就可以算出一个相同的会话密钥。</p><p><img src="http://blog.qiniu.lololo.store/2016-09-23-14746107556067.jpg" alt=""></p><p>该算法最大的问题在于不具备身份认证能力，很容易受到中间人攻击。因此在 HTTPS 中需配合使用 RSA 证书来做身份认证。</p><blockquote><p>注：<br>DH 算法是一种密钥交换算法。<br>RSA 既可作为密钥交换算法，又可以作为签名认证算法。</p></blockquote><h2 id="安全性"><a href="#安全性" class="headerlink" title="安全性"></a>安全性</h2><h3 id="前向安全性"><a href="#前向安全性" class="headerlink" title="前向安全性"></a>前向安全性</h3><p>前向安全性指的是，如果攻击者抓取并保存流量，那么将来私钥泄露后，攻击者也无法利用泄露的私钥解密这些流量。</p><p>斯诺登就曾曝光过 NSA 的『今日捕获，明日破解』政策。</p><p>RSA 协商密钥的安全性完全依赖于服务器端私钥的安全性，如果服务器端私钥泄露，则所有链接都不再安全。</p><p>而 DH 算法由于密钥协商过程中随机数并没有传输，因此即使私钥泄露，也无法获得会话的随机数，故而也就得不到会话密钥。因此是前向安全的。</p><h3 id="TLS-层存在的安全隐患"><a href="#TLS-层存在的安全隐患" class="headerlink" title="TLS 层存在的安全隐患"></a>TLS 层存在的安全隐患</h3><h4 id="降级"><a href="#降级" class="headerlink" title="降级"></a>降级</h4><p>这种方式就是攻击者充当中间人和服务器通信，然后把相应的通信内容通过 HTTP 协议发送给客户端，由于 HTTP 协议是未加密的，于是就可以截获用户的访问数据。</p><p>这种攻击方式比较简单，通过代理，可以很容易的把 HTTPS 变成 HTTP，这个一方面需要用户留意网站是否有从 HTTPS 跳转到 HTTP 的行为，另一方面服务器也可以通过配置将所有HTTP的请求强制转移到HTTPS上。</p><p>另一种是加密协议降级，如劫持后在 client hello 时只提供有漏洞的加密协议。这种方式可以在服务器端禁用低安全性的协议来规避。</p><h4 id="HTTPS-劫持"><a href="#HTTPS-劫持" class="headerlink" title="HTTPS 劫持"></a>HTTPS 劫持</h4><p>这种方式攻击者为了获得 HTTPS 的明文传输内容，需要充当中间人，替换服务器发给用户的包含公钥的证书。攻击者既和用户之间建立了 HTTPS 链接，又和服务器建立了 HTTPS 链接。</p><p>在上面握手建立的过程中，由于用户的公钥是攻击者生成的，所以攻击者可以轻易获得握手中的数据。也就可以获取到和用户通信过程中的对称加密的密钥，攻击者可以通过密钥获取用户发送的数据，同时在使用和服务器通信的密钥加密后再发送给服务器。</p><p>这种攻击方式也有一个明显的问题就是攻击者生成的证书几乎是不可能被用户信任的，在这种情况下，用户浏览器通常会提示该网站的证书不可信，是否继续访问，这已经对用户进行了一个明显的警告了。</p><p>当然，如果 CA 机构对证书的管理混乱也有可能造成这种劫持，如：</p><ol><li>谷歌官方安全博客<a href="https://security.googleblog.com/2015/03/maintaining-digital-certificate-security.html" target="_blank" rel="noopener">报道</a>，谷歌发现CNNIC颁发了多个针对谷歌域名的用于中间人攻击的证书。</li><li><a href="http://k.sina.cn/article_1772191555_69a17f430190016i6.html?vt=4" target="_blank" rel="noopener">国内CA机构沃通错误颁发GitHub域名SSL证书</a></li><li>震网病毒曾盗取台湾某证书机构的数字证书，用来给自己的病毒签名。</li></ol><p>另外我们也可以通过这种对基于 HTTPS 的通信进行抓包分析。Mac 平台著名的抓包工具 Charles 就是基于这种方式，首先要求你信任一个它的证书，然后自己充当中间人对你与某个服务器的 HTTPS 通信进行抓包分析。</p><h4 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h4><ol><li><a href="https://zh.wikipedia.org/zh-hans/%E5%BF%83%E8%84%8F%E5%87%BA%E8%A1%80%E6%BC%8F%E6%B4%9E" target="_blank" rel="noopener">OpenSSL 心脏出血漏洞</a></li><li><a href="http://www.infoq.com/cn/news/2015/05/sourceDNA-AFNetworking-ssl" target="_blank" rel="noopener">知名开源库AFNetworking曝SSL漏洞，2.5万iOS应用受影响</a></li><li><a href="http://bobao.360.cn/news/detail/2316.html" target="_blank" rel="noopener">Diffie-Hellman密钥交换协议存在安全缺陷</a></li><li><a href="http://netsec.ccert.edu.cn/duanhx/?p=2061" target="_blank" rel="noopener">NSA如何窃听Google的加密流量——当HTTPS遇到CDN</a></li></ol><h2 id="QA"><a href="#QA" class="headerlink" title="QA"></a>QA</h2><p><strong>1. 为何需要三个参数生成 Master Secret ？</strong></p><p>由于SSL协议中证书是静态的，因此需要引入一种随机因素来保证协商出来的密钥的随机性。SSL协议不信任每个主机都能生成完全随机的随机数，所以这里需要服务器和客户端共生成3个随机数，每增加一个自由度，随机性就会相应增加。</p><p>同时需要注意前两个随机数都是明文传输的，窃听者是可以轻易获取到的，只有最后一个 PreMaster Secret 是加密传输的，只有拥有服务器私钥才能解密，一旦 PreMaster Secret 泄露，那么本次通信就就完全可被破解了。</p><p><strong>2. 为何使用 RSA 协商密钥不直接使用证书公钥，而是通过server key exchange传输服务器密钥？</strong></p><p>可能有几个原因</p><ol><li>曾经美帝有出口限制，限制 RSA 在512比特以下，对称加密密钥限制40比特以下，如果超过512只能拿来签名，不能拿来加密。因此证书里头的RSA公钥长度超过512的不得不重新生成一对512长度的临时加密用的密钥，并将RSA公钥塞进消息体发出去。</li><li>服务器的证书只用做签名，不用做密钥交换，或者服务器没有证书。</li></ol><p><strong>3. Change Cipher Spec 有必要么？</strong></p><p>Change Cipher Spec 用来通知对端，开始启用协商好的密钥做对称加密，内容只有1个字节。 这个协议是冗余的，在TLS 1.3里面直接被删除了。</p><h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><p><a href="https://zh.wikipedia.org/wiki/%E8%BF%AA%E8%8F%B2-%E8%B5%AB%E7%88%BE%E6%9B%BC%E5%AF%86%E9%91%B0%E4%BA%A4%E6%8F%9B" target="_blank" rel="noopener">Deffie-Hellman 密钥交换</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;这是内部分享的资料，也是对前面几篇关于 HTTPS 相关文章的一个总结。现公开出来。前面的两篇关于 HTTPS 的文章链接如下：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;http://honglu.me/2016/01/13/HTTPS%E8%AF%A6%E8%A7%A3/&quot;&gt;HTTPS 详解&lt;/a&gt;  &lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;http://honglu.me/2016/10/03/HTTPS%E8%AF%A6%E8%A7%A3(%E4%BA%8C)/&quot;&gt;HTTPS 详解(二)&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;下面是我使用 wireshark 进行抓包的文件：&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;http://blog.qiniu.lololo.store/package.pcapng&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;package&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;可以下载下来使用 wireshark 打开后对比来看。&lt;/p&gt;
    
    </summary>
    
      <category term="HTTP" scheme="https://honglu.me/categories/HTTP/"/>
    
    
      <category term="HTTPS" scheme="https://honglu.me/tags/HTTPS/"/>
    
  </entry>
  
  <entry>
    <title>HTTPS 详解(二)</title>
    <link href="https://honglu.me/2016/10/03/HTTPS%E8%AF%A6%E8%A7%A3(%E4%BA%8C)/"/>
    <id>https://honglu.me/2016/10/03/HTTPS详解(二)/</id>
    <published>2016-10-03T02:00:00.000Z</published>
    <updated>2019-06-12T06:17:51.235Z</updated>
    
    <content type="html"><![CDATA[<p>HTTPS 是在 TCP 和 HTTP 之间增加了 TLS（Transport Layer Security，传输层安全），提供了内容加密、身份认证和数据完整性三大功能。TLS 的前身是 SSL（Secure Sockets Layer，安全套接字层），由网景公司开发，后来被 IETF 标准化并改名。</p><p>前面写过一篇 <a href="http://honglu.me/2016/01/13/HTTPS%E8%AF%A6%E8%A7%A3/">HTTPS 详解</a>，这一篇作为其补充，主要记录一下 HTTPS 的回话(密钥)重用、如何使用 CDN，以及 TLS 握手阶段的 Diffie-Hellman 算法。</p><a id="more"></a><h2 id="回话-密钥-重用"><a href="#回话-密钥-重用" class="headerlink" title="回话(密钥)重用"></a>回话(密钥)重用</h2><p>HTTP 建立 TCP 链接需要进行三次握手，HTTPS 在 TCP 之上又加上了 TLS 的握手过程，也就是说单建立链接的过程是相当耗时的。如果每次请求建立链接都要进行这样繁琐的握手过程，那显然是不能忍的，所以就提出了对 TLS 已经协商好的密钥重用。有两种方法，一种叫做 session ID 另一种叫做 session ticket。</p><p>session ID的思想很简单，就是每一次对话都有一个编号（session ID）。如果对话中断，下次重连的时候，只要客户端给出这个编号，且服务器有这个编号的记录，双方就可以重新使用已有的”对话密钥”，而不必重新生成一把。</p><p><img src="http://blog.qiniu.lololo.store/2016-09-23-14746012864410.png" alt=""></p><p>上图中，客户端给出session ID，服务器确认该编号存在，双方就不再进行握手阶段剩余的步骤，而直接用已有的对话密钥进行加密通信。</p><p>session ID的缺点在于session ID往往只保留在一台服务器上。所以，如果客户端的请求发到另一台服务器，就无法恢复对话。session ticket就是为了解决这个问题而诞生的。</p><p><img src="http://blog.qiniu.lololo.store/2016-09-23-14746013151393.png" alt=""></p><p>上图中，客户端不再发送session ID，而是发送一个服务器在上一次对话中发送过来的session ticket。这个session ticket是加密的，只有服务器才能解密，其中包括本次对话的主要信息，比如对话密钥和加密方法。当服务器收到session ticket以后，解密后就不必重新生成对话密钥了。</p><h2 id="如何使用-CDN"><a href="#如何使用-CDN" class="headerlink" title="如何使用 CDN"></a>如何使用 CDN</h2><p>网站为了加快访问速度通常会使用 CDN，如果使用外部 CDN，我们通常不会把私钥交给 CDN 服务商，针对这种场景 CloudFlare 公司提出了一种解决方案 —— Keyless SSL。</p><p><img src="http://blog.qiniu.lololo.store/2016-09-23-14746019657998.png" alt=""></p><p>上图中主要区别在于第四步，CDN 提供商拿到前一步由源服务器公钥加密过的密文后，通过安全的信道交给源服务器来解密，由源服务器提取会话秘钥或完成签名以后再提交给 CDN 节点。如此 CDN 就获得了此次回话的密钥。</p><h2 id="Diffie-Hellman-算法"><a href="#Diffie-Hellman-算法" class="headerlink" title="Diffie-Hellman 算法"></a>Diffie-Hellman 算法</h2><p>前一篇文章主要讲了 SSL 协议的握手过程，由于握手交换随机数的过程是不加密的，因此，如果有人窃听通信，他可以知道双方选择的加密方法，以及三个随机数中的两个。整个通话的安全，只取决于第三个随机数（Premaster secret）能不能被破解。</p><p>虽然理论上，只要服务器的公钥足够长（比如2048位），那么Premaster secret可以保证不被破解。但是为了足够安全，我们可以考虑把握手阶段的算法从默认的RSA算法，改为 Diffie-Hellman算法（简称DH算法）。</p><p>采用DH算法后，Premaster secret不需要传递，双方只要交换各自的参数，就可以算出一个相同的会话密钥。</p><p><img src="http://blog.qiniu.lololo.store/2016-09-23-14746107556067.jpg" alt="DH 算法原理"></p><p>Diffie-Hellman 算法把传递 PreMaster Secret 变成了传递 DH parameter。然后服务器和客户端根据 DH parameter 计算得出一个相同的会话密钥。</p><h2 id="参考-amp-扩展阅读"><a href="#参考-amp-扩展阅读" class="headerlink" title="参考 &amp; 扩展阅读"></a>参考 &amp; 扩展阅读</h2><p><a href="http://wetest.qq.com/lab/view/110.html" target="_blank" rel="noopener">HTTPS为什么安全 &amp;分析 HTTPS 连接建立全过程</a><br><a href="http://netsec.ccert.edu.cn/duanhx/?p=2061" target="_blank" rel="noopener">NSA如何窃听Google的加密流量——当HTTPS遇到CDN</a><br><a href="http://www.ruanyifeng.com/blog/2014/09/ssl-latency.html" target="_blank" rel="noopener">SSL延迟有多大？</a><br><a href="http://zhihu.com/question/21518760/answer/19698894" target="_blank" rel="noopener">HTTPS 要比 HTTP 多用多少服务器资源？</a><br><a href="http://wsfdl.com/algorithm/2016/02/04/%E7%90%86%E8%A7%A3Diffie-Hellman%E5%AF%86%E9%92%A5%E4%BA%A4%E6%8D%A2%E7%AE%97%E6%B3%95.html" target="_blank" rel="noopener">理解 Deffie-Hellman 密钥交换算法</a><br><a href="http://bobao.360.cn/news/detail/2316.html" target="_blank" rel="noopener">Diffie-Hellman密钥交换协议存在安全缺陷</a><br><a href="http://www.ruanyifeng.com/blog/2014/09/illustration-ssl.html" target="_blank" rel="noopener">图解SSL/TLS协议</a></p><p>最后补充一下：</p><p>从2017年1月1日起，苹果强制要求 APP 内使用 HTTPS，但是就国内目前的互联网环境来说，启用 HTTPS 的网站却只有很小一部分。由于 GFW 和 国内运营商肆无忌惮的 HTTP 劫持等，其实使用 HTTPS 是有诸多好处的，然并卵，没有强有力的人来推动，HTTPS 始终难以普及，但愿这次苹果的强力措施能起到明显效果吧~</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;HTTPS 是在 TCP 和 HTTP 之间增加了 TLS（Transport Layer Security，传输层安全），提供了内容加密、身份认证和数据完整性三大功能。TLS 的前身是 SSL（Secure Sockets Layer，安全套接字层），由网景公司开发，后来被 IETF 标准化并改名。&lt;/p&gt;
&lt;p&gt;前面写过一篇 &lt;a href=&quot;http://honglu.me/2016/01/13/HTTPS%E8%AF%A6%E8%A7%A3/&quot;&gt;HTTPS 详解&lt;/a&gt;，这一篇作为其补充，主要记录一下 HTTPS 的回话(密钥)重用、如何使用 CDN，以及 TLS 握手阶段的 Diffie-Hellman 算法。&lt;/p&gt;
    
    </summary>
    
      <category term="HTTP" scheme="https://honglu.me/categories/HTTP/"/>
    
    
      <category term="HTTPS" scheme="https://honglu.me/tags/HTTPS/"/>
    
  </entry>
  
  <entry>
    <title>一张图片引发的深思</title>
    <link href="https://honglu.me/2016/09/02/%E4%B8%80%E5%BC%A0%E5%9B%BE%E7%89%87%E5%BC%95%E5%8F%91%E7%9A%84%E6%B7%B1%E6%80%9D/"/>
    <id>https://honglu.me/2016/09/02/一张图片引发的深思/</id>
    <published>2016-09-02T10:00:00.000Z</published>
    <updated>2018-08-09T03:55:11.819Z</updated>
    
    <content type="html"><![CDATA[<p>在项目开发中使用 <code>SDWebImage</code> 来做图片缓存加载，但是遇到一个问题就是服务器的某一张图片始终无法加载出来，通过URL拿到原图之后可以正常查看。于是追根溯源定位到了 SDWebImage 中的 <code>decodedImageWithImage</code> 方法中，也给自己科普了一下图像处理的相关知识。</p><p>下面先了解图像的一些基础知识，然后分析遇到的问题。</p><a id="more"></a><h2 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h2><p>图像可以分为矢量图和位图，我们通常使用的图像为位图格式，这种格式又分为几种颜色模型，如 RGB,CMYK 等，分别适用于不同的场景。</p><p>我们常用的 RGB 则是通过颜色发光原理来设计的。其分为红(R)、绿(G)、蓝(B)三个颜色通道，每个通道的数值表示该通道的明暗程度，根据单位像素所占空间不同，其又可以分为RGB1,RGB2,RGB4,RGB8,RGB16,RGB24,RGB32等多种格式，其中RGB1、RGB2,RGB4,RGB8为调色板类型的RGB格式，即需要通过颜色索引表来描述颜色信息，RGB16为高彩色(Hi Color)，RGB24为真彩色(TRUE COLOR)，RGB32则带Aphal通道(RGBA)。</p><p>对于 RGB16，其实也是通过调色板实现的，可以分为 RGB444,RGB565,RGB555三种方式，后面的三个数字分别表示三个通道的数据位数，RGB565 表示红(R)、蓝(B)各占5位，绿(G)占三位，即单像素的表示方式为：<code>RRRRRGGGGGGBBBBB</code>，之所以绿色使用6位是因为人眼对绿色的辨识程度比较高。而RGB555表示为 <code>XRRRRRGGGGGBBBBB</code> ，其中 X 表示该位不使用，由于每个颜色使用5位表示，所以每个通道最多包含32种不同亮度值，相关的索引表也就需要把亮度从明到暗均分成32份，可以通过使用屏蔽字和移位操作来得到RGB各分量的值：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RGB555_MASK_RED 0x7C00</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RGB555_MASK_GREEN 0x03E0</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RGB555_MASK_BLUE 0x001F</span></span><br><span class="line">R = (wPixel &amp; RGB555_MASK_RED) &gt;&gt; <span class="number">10</span>; <span class="comment">// 取值范围0-31</span></span><br><span class="line">G = (wPixel &amp; RGB555_MASK_GREEN) &gt;&gt; <span class="number">5</span>; <span class="comment">// 取值范围0-31</span></span><br><span class="line">B = wPixel &amp; RGB555_MASK_BLUE; <span class="comment">// 取值范围0-31</span></span><br></pre></td></tr></table></figure><p>然后通过 RGB 的取值在索引表中得到真实的颜色。</p><p>索引颜色是一种位图图像的编码方法。当真彩色图片转换为索引颜色的图片时，如果原图颜色不在索引颜色中，计算机会从索引颜色中选出一个相近的颜色来模拟该颜色(抖动到相近的颜色)，这也是早期浏览器存在 web 安全色的原因。要了解 web 安全色 的历史，可以戳<a href="http://www.lhyum.com/%E8%B0%88%E8%B0%88web%E5%AE%89%E5%85%A8%E8%89%B2%EF%BC%88web-safe-color%EF%BC%89/" target="_blank" rel="noopener">这里</a></p><p>对于 RGB24 使用24位表示一个像素，RGB分量都用8位表示，取值范围为0-255。RGB32 则多了一个8位的 alpha 通道来表示透明度，可以使用RGBQUAD数据结构来操作一个像素：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">tagRGBQUAD</span> &#123;</span></span><br><span class="line">BYTE rgbBlue; <span class="comment">// 蓝色分量</span></span><br><span class="line">BYTE rgbGreen; <span class="comment">// 绿色分量</span></span><br><span class="line">BYTE rgbRed; <span class="comment">// 红色分量</span></span><br><span class="line">BYTE rgbReserved; <span class="comment">// 保留字节（用作Alpha通道或忽略）</span></span><br><span class="line">&#125; RGBQUAD。</span><br></pre></td></tr></table></figure><p>显然，对于 RGB24 和 RGB32 ，每个通道都使用8位表示，即每个通道有256中不同的色彩深度。那么，如果每个通道使用16位甚至32位来表示，那就用有更多的颜色深度了。包含 32 位/通道的图像也称作<a href="https://zh.wikipedia.org/wiki/%E9%AB%98%E5%8A%A8%E6%80%81%E8%8C%83%E5%9B%B4%E6%88%90%E5%83%8F" target="_blank" rel="noopener">高动态范围(HDR)图像</a>。</p><h2 id="诡异的图片问题"><a href="#诡异的图片问题" class="headerlink" title="诡异的图片问题"></a>诡异的图片问题</h2><p>定位到 SDWebImage 中问题代码位于 <code>SDWebImageDecoder</code> 文件下的 <code>decodedImageWithImage</code> 函数，截取部分如下：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">CGImageRef</span> imageRef = image.CGImage;</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">size_t width = <span class="built_in">CGImageGetWidth</span>(imageRef);</span><br><span class="line">size_t height = <span class="built_in">CGImageGetHeight</span>(imageRef);</span><br><span class="line"><span class="comment">// current</span></span><br><span class="line"><span class="built_in">CGColorSpaceModel</span> imageColorSpaceModel = <span class="built_in">CGColorSpaceGetModel</span>(<span class="built_in">CGImageGetColorSpace</span>(imageRef));</span><br><span class="line"><span class="built_in">CGColorSpaceRef</span> colorspaceRef = <span class="built_in">CGImageGetColorSpace</span>(imageRef);</span><br><span class="line"></span><br><span class="line"><span class="keyword">bool</span> unsupportedColorSpace = (imageColorSpaceModel == <span class="number">0</span> || imageColorSpaceModel == <span class="number">-1</span> || imageColorSpaceModel == kCGColorSpaceModelCMYK || imageColorSpaceModel == kCGColorSpaceModelIndexed);</span><br><span class="line"><span class="keyword">if</span> (unsupportedColorSpace)</span><br><span class="line">    colorspaceRef = <span class="built_in">CGColorSpaceCreateDeviceRGB</span>();</span><br><span class="line"></span><br><span class="line"><span class="built_in">CGContextRef</span> context = <span class="built_in">CGBitmapContextCreate</span>(<span class="literal">NULL</span>, width,</span><br><span class="line">                                             height,</span><br><span class="line">                                             <span class="built_in">CGImageGetBitsPerComponent</span>(imageRef),</span><br><span class="line">                                             <span class="number">0</span>,</span><br><span class="line">                                             colorspaceRef,</span><br><span class="line">                                             kCGBitmapByteOrderDefault | kCGImageAlphaPremultipliedFirst);</span><br></pre></td></tr></table></figure><p>测试发现，问题图片会导致创建的 context 为 nil。</p><p>查看 <code>CGBitmapContextCreate</code> 的函数定义：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">CG_EXTERN</span> <span class="built_in">CGContextRef</span> __<span class="keyword">nullable</span> <span class="built_in">CGBitmapContextCreate</span>(<span class="keyword">void</span> * __<span class="keyword">nullable</span> data,</span><br><span class="line">    size_t width, size_t height, size_t bitsPerComponent, size_t bytesPerRow,</span><br><span class="line">    <span class="built_in">CGColorSpaceRef</span> __<span class="keyword">nullable</span> space, uint32_t bitmapInfo)</span><br><span class="line">    <span class="built_in">CG_AVAILABLE_STARTING</span>(__MAC_10_0, __IPHONE_2_0);</span><br></pre></td></tr></table></figure><blockquote><p>Create a bitmap context. The context draws into a bitmap which is <code>width</code> pixels wide and <code>height</code> pixels high. The number of components for each pixel is specified by <code>space</code>, which may also specify a destination color profile. The number of bits for each component of a pixel is specified by <code>bitsPerComponent</code>. The number of bytes per pixel is equal to <code>(bitsPerComponent * number of components + 7)/8</code>. Each row of the bitmap consists of <code>bytesPerRow</code> bytes, which must be at least <code>width * bytes per pixel</code> bytes; in addition, <code>bytesPerRow</code> must be an integer multiple of the number of bytes per pixel. <code>data</code>, if non-NULL, points to a block of memory at least <code>bytesPerRow * height</code> bytes. If <code>data</code> is NULL, the data for context is allocated automatically and freed when the context is deallocated. <code>bitmapInfo</code> specifies whether the bitmap should contain an alpha channel and how it’s to be generated, along with whether the components are floating-point or integer.</p></blockquote><p>翻译如下：</p><blockquote><p>创建一个位图 context，位图的像素通过 <code>width</code>、<code>hight</code> 指定。每一个像素的颜色个数(number of components 通道数)通过 <code>space</code> 指定，也可以通过一个颜色描述文件来指定。每像素中每个颜色占用的位空间(bit)通过 <code>bitsPerComponent</code> 参数指定。每个像素的字节数(bytes per pixel)通过公式 <code>(bitsPerComponent * number of components + 7)/8</code> 计算(这个加7应该是为了位对齐)。位图的每一行包含 <code>bytesPerRow</code> 字节，其最少需要 <code>width * bytes per pixel</code> 字节；此外，<code>bytesPerRow</code> 必须是 <code>bytes per pixel</code> 的整数倍。<code>data</code> 如果不为空，其指向一个至少 <code>bytesPerRow * height</code> 字节的内存空间。若 <code>data</code> 为空，context 的 data 会随着该 context 自动创建和销毁。<code>bitmapInfo</code> 指出该位图是否包含 alpha 通道和它是如何产生的(RGB/RGBA/RGBX…)，还有每个通道应该用整数标识还是浮点数。</p></blockquote><p>代码中 <code>bitsPerComponent</code> 参数是获取的原始图像的 bitsPerComponent 值， <code>bytesPerRow</code> 参数直接赋值 0。</p><p>通过对问题图片测试发现其 bitsPerComponent 为 16，也就是说每个通道是由16位构成的。另外，apple 在 <a href="https://developer.apple.com/library/mac/documentation/GraphicsImaging/Conceptual/drawingwithquartz2d/dq_context/dq_context.html" target="_blank" rel="noopener">这里</a> 有份文档，里面注明了 OS X 以及 iOS 中位图所支持的像素格式，发现在 iOS 下苹果最高只支持8位的bpc。实际上问题就出在这里了，原图的 bpc 是16位，iOS 并不支持，所以得到的 context 是 nil。</p><p>作者修改后为</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">size_t width = <span class="built_in">CGImageGetWidth</span>(imageRef);</span><br><span class="line">size_t height = <span class="built_in">CGImageGetHeight</span>(imageRef);</span><br><span class="line"><span class="built_in">NSUInteger</span> bytesPerPixel = <span class="number">4</span>;</span><br><span class="line"><span class="built_in">NSUInteger</span> bytesPerRow = bytesPerPixel * width;</span><br><span class="line"><span class="built_in">NSUInteger</span> bitsPerComponent = <span class="number">8</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">CGContextRef</span> context = <span class="built_in">CGBitmapContextCreate</span>(<span class="literal">NULL</span>,</span><br><span class="line">                                            width,</span><br><span class="line">                                            height,</span><br><span class="line">                                            bitsPerComponent,</span><br><span class="line">                                            bytesPerRow,</span><br><span class="line">                                            colorspaceRef,</span><br><span class="line">                                            kCGBitmapByteOrderDefault | kCGImageAlphaNoneSkipLast);</span><br></pre></td></tr></table></figure><p>直接把 bpp 和 bpc 指定为4和8，既每个像素由四通道构成，每通道8位。然后后面 <code>CGContextDrawImage(context, CGRectMake(0, 0, width, height), imageRef)</code> 的时候实际上是减少了 image 的每通道位数。同时，由于 <code>bitmapInfo</code> 参数设置为 <code>kCGBitmapByteOrderDefault | kCGImageAlphaNoneSkipLast</code>，意味着新的位图图像是不使用后面8位的 alpha 通道的。</p><h2 id="关于-decodedImageWithImage-方法"><a href="#关于-decodedImageWithImage-方法" class="headerlink" title="关于 decodedImageWithImage 方法"></a>关于 decodedImageWithImage 方法</h2><p>在我们使用 UIImage 的时候，创建的图片通常不会直接加载到内存，而是在渲染的时候再进行解压并加载到内存。这就会导致 UIImage 在渲染的时候效率上不是那么高效。为了提高效率通过 <code>decodedImageWithImage</code> 方法把图片提前解压加载到内存，这样这张新图片就不再需要重复解压了，提高了渲染效率。这是一种空间换时间的做法。</p><p>在UI渲染的时候，实际上是把多个图层按像素叠加计算的过程，需要对每一个像素进行 RGBA 的叠加计算。当某个 layer 的是不透明的，也就是 opaque 为 YES 时，GPU 可以直接忽略掉其下方的图层，这就减少了很多工作量。这也是调用 <code>CGBitmapContextCreate</code> 时 bitmapInfo 参数设置为忽略掉 alpha 通道的原因。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://developer.apple.com/library/mac/documentation/GraphicsImaging/Conceptual/drawingwithquartz2d/dq_context/dq_context.html" target="_blank" rel="noopener">Quartz 2D Programming Guide</a><br><a href="https://objccn.io/issue-3-1/" target="_blank" rel="noopener">绘制像素到屏幕上</a><br><a href="https://zh.wikipedia.org/wiki/%E8%89%B2%E5%BD%A9%E7%A9%BA%E9%96%93" target="_blank" rel="noopener">wikipedia - 色彩空间</a><br><a href="https://zh.wikipedia.org/wiki/%E4%B8%89%E5%8E%9F%E8%89%B2%E5%85%89%E6%A8%A1%E5%BC%8F" target="_blank" rel="noopener">wikipedia - 三原色光模式</a><br><a href="http://baike.baidu.com/item/RGB" target="_blank" rel="noopener">百度百科 - RGB</a><br><a href="http://blog.csdn.net/jacklam200/article/details/5972724" target="_blank" rel="noopener">RGB模型</a><br><a href="http://longtimenoc.com/archives/ios%E5%A6%82%E4%BD%95%E9%81%BF%E5%85%8D%E5%9B%BE%E5%83%8F%E8%A7%A3%E5%8E%8B%E7%BC%A9%E7%9A%84%E6%97%B6%E9%97%B4%E5%BC%80%E9%94%80" target="_blank" rel="noopener">如何避免图像解压缩的时间开销</a><br><a href="http://blog.ibireme.com/2015/11/02/ios_image_tips/" target="_blank" rel="noopener">iOS 处理图片的一些小 Tip</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在项目开发中使用 &lt;code&gt;SDWebImage&lt;/code&gt; 来做图片缓存加载，但是遇到一个问题就是服务器的某一张图片始终无法加载出来，通过URL拿到原图之后可以正常查看。于是追根溯源定位到了 SDWebImage 中的 &lt;code&gt;decodedImageWithImage&lt;/code&gt; 方法中，也给自己科普了一下图像处理的相关知识。&lt;/p&gt;
&lt;p&gt;下面先了解图像的一些基础知识，然后分析遇到的问题。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="iOS" scheme="https://honglu.me/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>Laravel 5.2 使用 JWT 进行多用户认证</title>
    <link href="https://honglu.me/2016/08/20/Laravel%E4%BD%BF%E7%94%A8JWT%E5%A4%9A%E7%94%A8%E6%88%B7%E8%AE%A4%E8%AF%81/"/>
    <id>https://honglu.me/2016/08/20/Laravel使用JWT多用户认证/</id>
    <published>2016-08-20T07:00:00.000Z</published>
    <updated>2018-08-09T03:55:11.788Z</updated>
    
    <content type="html"><![CDATA[<p>Laravel 5.2 中本身自带了 auth 认证，也是支持多用户的，但是项目使用 Angular，所以后端只需要以接口的形式提供 json api 就可以了，这里就需要使用 JWT 来进行用户认证了，网上普遍的方式是使用 JWT-Auth 框架来进行 JWT 认证，但是发现 5.9 版本不支持多用户认证，1.0 之后才支持。但是由于 1.0 版本是 alpha 版本，所以 github 上并没有其相关文档，找了半天发现作者在某个 issue 里面提到了多用户的使用方法，遂在这里整理记录一下。</p><a id="more"></a><p>1 在 <code>composer.json</code> 文件的 require 中添加如下行来引入 JWT-Auth</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">"require": &#123;</span><br><span class="line">    ...</span><br><span class="line">    "tymon/jwt-auth": "^1.0@dev",</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2 comfig/app.php 配置</p><p><code>providers</code> 下添加 </p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">Tymon\JWTAuth\Providers\LaravelServiceProvider::class</span><br></pre></td></tr></table></figure><p><code>aliases</code> 下添加 </p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="string">'JWTAuth'</span> =&gt; Tymon\JWTAuth\Facades\JWTAuth::class,</span><br><span class="line"><span class="string">'JWTFactory'</span> =&gt; Tymon\JWTAuth\Facades\JWTFactory::class,</span><br></pre></td></tr></table></figure><p>3 terminal 下执行 <code>php artisan vendor:publish --provider=&quot;Tymon\JWTAuth\Providers\LaravelServiceProvider&quot;</code> 在 config 下生成 <code>jwt.php</code> 配置文件</p><p>4 terminal 下执行 <code>php artisan jwt:secret</code> 生成加密密钥</p><p>5 config/auth.php 设置相关参数</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="string">'guards'</span> =&gt; [</span><br><span class="line">    ...</span><br><span class="line">    <span class="string">'admin'</span> =&gt; [</span><br><span class="line">            <span class="string">'driver'</span> =&gt; <span class="string">'jwt'</span>,</span><br><span class="line">            <span class="string">'provider'</span> =&gt; <span class="string">'admins'</span>,</span><br><span class="line">        ],</span><br><span class="line">],</span><br><span class="line"></span><br><span class="line"><span class="string">'providers'</span> =&gt; [</span><br><span class="line">    ...</span><br><span class="line">    <span class="string">'admins'</span> =&gt; [</span><br><span class="line">        <span class="string">'driver'</span> =&gt; <span class="string">'eloquent'</span>,</span><br><span class="line">        <span class="string">'model'</span> =&gt; App\Models\Admin::class,</span><br><span class="line">    ],</span><br><span class="line">],</span><br></pre></td></tr></table></figure><p>6 route.php 中添加登录相关路由</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">Route::post(<span class="string">'/auth/login'</span>, <span class="string">'AuthController@login'</span>);</span><br></pre></td></tr></table></figure><p>7 AuthController.php 中添加登录逻辑</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">App</span>\<span class="title">Http</span>\<span class="title">Controllers</span>\<span class="title">Admin</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> <span class="title">App</span>\<span class="title">Models</span>\<span class="title">Admin</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">Illuminate</span>\<span class="title">Http</span>\<span class="title">Request</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">App</span>\<span class="title">Http</span>\<span class="title">Controllers</span>\<span class="title">Controller</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">Illuminate</span>\<span class="title">Foundation</span>\<span class="title">Auth</span>\<span class="title">ThrottlesLogins</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">Illuminate</span>\<span class="title">Foundation</span>\<span class="title">Auth</span>\<span class="title">AuthenticatesAndRegistersUsers</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">Validator</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">JWTAuth</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">Tymon</span>\<span class="title">JWTAuth</span>\<span class="title">Exceptions</span>\<span class="title">JWTException</span>;</span><br><span class="line"><span class="keyword">use</span> <span class="title">Auth</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AuthController</span> <span class="keyword">extends</span> <span class="title">Controller</span></span>&#123;</span><br><span class="line">    <span class="keyword">use</span> <span class="title">AuthenticatesAndRegistersUsers</span>, <span class="title">ThrottlesLogins</span>;</span><br><span class="line">    <span class="keyword">protected</span> $guard = <span class="string">'admin'</span>;</span><br><span class="line">    <span class="keyword">protected</span> $username = <span class="string">'username'</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 注册</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">register</span><span class="params">(Request $request)</span></span>&#123;</span><br><span class="line">        $validator = <span class="keyword">$this</span>-&gt;validator($request-&gt;all());</span><br><span class="line">        <span class="keyword">if</span> ($validator-&gt;fails()) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;errorBadRequest($validator-&gt;messages(), <span class="number">500</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        $user = <span class="keyword">$this</span>-&gt;create($request-&gt;all());</span><br><span class="line"></span><br><span class="line">        $credentials = [</span><br><span class="line">            <span class="string">'username'</span> =&gt; $request[<span class="string">'username'</span>],</span><br><span class="line">            <span class="string">'password'</span> =&gt; $request[<span class="string">'password'</span>],</span><br><span class="line">        ];</span><br><span class="line"></span><br><span class="line">        $token = Auth::guard(<span class="keyword">$this</span>-&gt;getGuard())-&gt;attempt($credentials);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;JSON(compact(<span class="string">'token'</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 登录</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">login</span><span class="params">(Request $request)</span></span>&#123;</span><br><span class="line">        <span class="keyword">$this</span>-&gt;validateLogin($request);</span><br><span class="line"></span><br><span class="line">        $throttles = <span class="keyword">$this</span>-&gt;isUsingThrottlesLoginsTrait();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ($throttles &amp;&amp; $lockedOut = <span class="keyword">$this</span>-&gt;hasTooManyLoginAttempts($request)) &#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;fireLockoutEvent($request);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;sendLockoutResponse($request);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        $credentials = <span class="keyword">$this</span>-&gt;getCredentials($request);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 这里改用 JWT-Auth 进行认证</span></span><br><span class="line">        <span class="keyword">if</span> ($token = Auth::guard(<span class="keyword">$this</span>-&gt;getGuard())-&gt;attempt($credentials)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;handleUserWasAuthenticated($request, $throttles, $token);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> ($throttles &amp;&amp; ! $lockedOut) &#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;incrementLoginAttempts($request);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;sendFailedLoginResponse($request);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 刷新 token</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">refreshToken</span><span class="params">()</span></span>&#123;</span><br><span class="line">        $oldToken = JWTAuth::getToken();</span><br><span class="line">        $token = JWTAuth::refresh($oldToken);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;JSON(compact(<span class="string">'token'</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 退出登录</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">logout</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Auth::guard(<span class="keyword">$this</span>-&gt;getGuard())-&gt;logout();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;JSON(<span class="keyword">array</span>(<span class="string">'status'</span> =&gt; <span class="string">'success'</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">validator</span><span class="params">(array $data)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Validator::make($data, Admin::$rules);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">create</span><span class="params">(array $data)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Admin::create([</span><br><span class="line">            <span class="string">'username'</span> =&gt; $data[<span class="string">'username'</span>],</span><br><span class="line">            <span class="string">'password'</span> =&gt; bcrypt($data[<span class="string">'password'</span>]),</span><br><span class="line">        ]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">handleUserWasAuthenticated</span><span class="params">(Request $request, $throttles, $token)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> ($throttles) &#123;</span><br><span class="line">            <span class="keyword">$this</span>-&gt;clearLoginAttempts($request);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (method_exists(<span class="keyword">$this</span>, <span class="string">'authenticated'</span>)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">$this</span>-&gt;authenticated($request, Auth::guard(<span class="keyword">$this</span>-&gt;getGuard())-&gt;user(), $token);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> redirect()-&gt;intended(<span class="keyword">$this</span>-&gt;redirectPath());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">authenticated</span><span class="params">($request, $user, $token)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">self</span>::JSON([</span><br><span class="line">            <span class="string">'user'</span>    =&gt; $user,</span><br><span class="line">            <span class="string">'token'</span>   =&gt; $token</span><br><span class="line">        ]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">sendFailedLoginResponse</span><span class="params">(Request $request)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">self</span>::errorBadRequest(<span class="keyword">$this</span>-&gt;getFailedLoginMessage());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 <code>AuthController</code> 中的 guard 属性可以指定在 <code>auth.php</code> 中声明的认证方式。</p><p>在其它需要认证才能访问的 controller 中可以使用中间件来判断</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">$this</span>-&gt;middleware(<span class="string">'auth:admin'</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>auth 中间件 <code>Authenticate.php</code></p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">handle</span><span class="params">($request, Closure $next, $guard = null)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (Auth::guard($guard)-&gt;guest()) &#123;</span><br><span class="line">        <span class="keyword">return</span> response(<span class="string">'Unauthorized.'</span>, <span class="number">401</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> $next($request);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://github.com/tymondesigns/jwt-auth/issues/513" target="_blank" rel="noopener">Feature: Laravel 5.2 Custom Authentication Guard and Driver</a><br><a href="https://github.com/tymondesigns/jwt-auth/issues/376" target="_blank" rel="noopener">Add support &amp; documentation for integrating with Lumen 5.2</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Laravel 5.2 中本身自带了 auth 认证，也是支持多用户的，但是项目使用 Angular，所以后端只需要以接口的形式提供 json api 就可以了，这里就需要使用 JWT 来进行用户认证了，网上普遍的方式是使用 JWT-Auth 框架来进行 JWT 认证，但是发现 5.9 版本不支持多用户认证，1.0 之后才支持。但是由于 1.0 版本是 alpha 版本，所以 github 上并没有其相关文档，找了半天发现作者在某个 issue 里面提到了多用户的使用方法，遂在这里整理记录一下。&lt;/p&gt;
    
    </summary>
    
      <category term="web" scheme="https://honglu.me/categories/web/"/>
    
    
      <category term="web" scheme="https://honglu.me/tags/web/"/>
    
      <category term="php" scheme="https://honglu.me/tags/php/"/>
    
  </entry>
  
  <entry>
    <title>移动端接口指南</title>
    <link href="https://honglu.me/2016/07/29/%E7%A7%BB%E5%8A%A8%E7%AB%AF%E6%8E%A5%E5%8F%A3%E6%8C%87%E5%8D%97/"/>
    <id>https://honglu.me/2016/07/29/移动端接口指南/</id>
    <published>2016-07-29T03:20:00.000Z</published>
    <updated>2018-08-09T03:55:11.826Z</updated>
    
    <content type="html"><![CDATA[<p>该文档参考 REST Web API 设计，但是考虑到目前服务器所使用技术完全使用 REST API 工作量过大。所以做了一些妥协。</p><p>加星号部分可根据需求选择性实现。</p><a id="more"></a><h2 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h2><p>首先描述一下 Request 和 Response 的大概格式</p><h3 id="Request"><a href="#Request" class="headerlink" title="Request"></a>Request</h3><figure class="highlight http"><table><tr><td class="code"><pre><span class="line"><span class="keyword">POST</span> <span class="string">/api</span> HTTP/1.1</span><br><span class="line"><span class="attribute">Host</span>: www.domain.com</span><br><span class="line"><span class="attribute">Accept</span>: application/vnd.company+json</span><br><span class="line"><span class="attribute">Accept-Language</span>: zh-cn</span><br><span class="line"><span class="attribute">Accept-Encoding</span>: gzip, deflate</span><br><span class="line"><span class="attribute">X-Api-Version</span>: 1.0</span><br><span class="line"><span class="attribute">User-Agent</span>: app/1.0 CFNetwork/760.6.3 iOS/9.3</span><br><span class="line"><span class="attribute">Connection</span>: keep-alive</span><br><span class="line"><span class="attribute">Content-type</span>: application/json</span><br><span class="line"><span class="attribute">Content-Length</span>: 1024</span><br><span class="line"><span class="attribute">Etag</span>: "qwghu2u3423kjgh234iughjkh"</span><br><span class="line"><span class="attribute">X-UUID</span>: 01234567-89ab-cdef-0123-456789abcdef</span><br><span class="line"><span class="attribute">X-Geo-Location</span>: +40.6894-074.0447</span><br><span class="line"></span><br><span class="line"><span class="attribute">content</span></span><br></pre></td></tr></table></figure><h3 id="Response"><a href="#Response" class="headerlink" title="Response"></a>Response</h3><figure class="highlight"><table><tr><td class="code"><pre><span class="line">HTTP/1.1 <span class="number">200</span> OK</span><br><span class="line"><span class="attribute">Date</span>: Thu, 28 Jul 2016 06:55:31 GMT</span><br><span class="line"><span class="attribute">Content-Type</span>: application/vnd.company+json;charset=utf-8</span><br><span class="line"><span class="attribute">Vary</span>: Accept-Encoding</span><br><span class="line"><span class="attribute">Server</span>: nginx/1.0.4</span><br><span class="line"><span class="attribute">Content-Encoding</span>: gzip</span><br><span class="line"><span class="attribute">Etag</span>: "qwghu2u3423kjgh234iughjkh"</span><br><span class="line"><span class="attribute">Content-Length</span>: 1024</span><br><span class="line"></span><br><span class="line">&#123;"code":200,"message":"success","data":&#123;"color":"WHITE","size":7.5&#125;&#125;</span><br></pre></td></tr></table></figure><h2 id="请求方式"><a href="#请求方式" class="headerlink" title="请求方式"></a>请求方式</h2><p>这里分为两种方案：</p><h3 id="REST"><a href="#REST" class="headerlink" title="REST"></a>REST</h3><p>该方案是 REST API 的请求模式</p><ul><li>GET (选择)：从服务器上获取一个具体的资源或者一个资源列表。 </li><li>POST （创建）： 在服务器上创建一个新的资源。</li><li>PUT（更新）：以整体的方式更新服务器上的一个资源。 </li><li>PATCH （更新）：只更新服务器上一个资源的一个属性。</li><li>DELETE（删除）：删除服务器上的一个资源。 </li><li>HEAD ： 获取一个资源的元数据，如数据的哈希值或最后的更新时间。</li><li>OPTIONS：获取客户端能对资源做什么操作的信息。</li></ul><h3 id="ONLY-GET-POST"><a href="#ONLY-GET-POST" class="headerlink" title="ONLY GET/POST"></a>ONLY GET/POST</h3><p>对于大部分服务器并没有很好的支持 PUT/DELETE 之类的方法，因此，只使用 GET/POST 方法，是个便捷省力的方式。</p><p>当然我们也可以把其它操作放到 Request Header 里面，只需要在 Header 里面加上 <code>x-Method</code> 字段就可以了。</p><h3 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h3><p>对于提交 body 中的数据，服务器只接受 <strong>JSON序列化</strong> 的数据，不接受 form-encoded 数据，这样可以保持请求和返回都是JSON数据。</p><p>注意各方法必须符合下表中 <strong>幂等</strong> 以及 <strong>安全</strong> 要求</p><table><thead><tr><th>HTTP</th><th>幂等</th><th>安全</th></tr></thead><tbody><tr><td>OPTIONS</td><td>yes</td><td>yes</td></tr><tr><td>GET</td><td>yes</td><td>yes</td></tr><tr><td>HEAD</td><td>yes</td><td>yes</td></tr><tr><td>PUT</td><td>yes</td><td>no</td></tr><tr><td>POST</td><td>no</td><td>no</td></tr><tr><td>DELETE</td><td>yes</td><td>no</td></tr><tr><td>PATCH</td><td>no</td><td>no</td></tr></tbody></table><p>安全：不改变服务器资源状态<br>幂等：请求的结果和请求次数无关</p><p>PUT 必须是对服务器资源的完整替换，而 PATCH 可以要求服务器执行逻辑操作(比如加减)，所以 PATCH 不是幂等的。</p><h2 id="Header"><a href="#Header" class="headerlink" title="Header"></a>Header</h2><h3 id="X-Api-Version"><a href="#X-Api-Version" class="headerlink" title="X-Api-Version"></a>X-Api-Version</h3><p>版本号通常有两种标识方式</p><p>一种是放到 header 里，如上面的:</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">X-Api-Version: 3.0</span><br></pre></td></tr></table></figure><p>另一种是直接在URL 添加版本号</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://api.domain.com/v3/</span><br></pre></td></tr></table></figure><p>第一种可以针对单个接口设置版本号，第二种是整套接口系统采用统一版本号。</p><p>第二种可用于对外开放 API，比如微博、github的 open API，使用统一版本号。</p><p>针对单个 APP 通常采用第一种。</p><h3 id="自定义-content-type"><a href="#自定义-content-type" class="headerlink" title="自定义 content-type"></a>自定义 content-type</h3><p>不要使用标准的 content-type ，因为客户端没有处理这些 content-type 的能力，可以使用下面的自定义格式</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Content-type: application/vnd.company+xml</span><br><span class="line">Content-type: application/vnd.company+json</span><br></pre></td></tr></table></figure><p>客户端可以根据这些特定的 content-type 写专门的解析方式。</p><p>所有 application/vnd 都是提供商自定的 content-type 而不是标准。仅应用于非标准 content-type。</p><h3 id="User-Agent"><a href="#User-Agent" class="headerlink" title="User-Agent"></a>User-Agent</h3><p><code>User-Agent</code> 采用 <code>APP名称/版本号 网络组件名/版本号 系统名/版本号</code> 的形式。</p><h3 id="x-UUID"><a href="#x-UUID" class="headerlink" title="x-UUID*"></a>x-UUID*</h3><p>这里发送设备的 UUID，方便服务器进行统计</p><p>UUID 使用 <code>01234567-89ab-cdef-0123-456789abcdef</code> 这种形式</p><h3 id="Etag"><a href="#Etag" class="headerlink" title="Etag*"></a>Etag*</h3><p>方便客户端进行数据缓存，后面有详细说明。</p><h3 id="x-Geo-Location"><a href="#x-Geo-Location" class="headerlink" title="x-Geo-Location*"></a>x-Geo-Location*</h3><p>该字段是发送用户当前地理坐标的，同样是方便服务器进行统计的。</p><p>当然，部分接口可能也会利用这个字段进行针对性的给用户推送地域性的信息，比如本地新闻</p><h2 id="BODY"><a href="#BODY" class="headerlink" title="BODY"></a>BODY</h2><h3 id="JSON-数据"><a href="#JSON-数据" class="headerlink" title="JSON 数据"></a>JSON 数据</h3><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"code"</span>:</span><br><span class="line">  <span class="string">"message"</span>:</span><br><span class="line">  <span class="string">"errors"</span>:</span><br><span class="line">  <span class="string">"data"</span>:</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>严格数据类型</li><li>去除空值，除非十分有必要存在，那么则使用 null</li><li>时间采用 <a href="https://www.iso.org/obp/ui/#iso:std:iso:8601:ed-3:v1:en" target="_blank" rel="noopener">ISO 8601</a> 标准格式 <code>YYYY-MM-DDTHH:MM:SSZ</code></li><li>只有成功时才返回 data，不成功则不返回 data。data 的数据类型限定为对象或数组</li><li>数据中的枚举采用字符枚举如：<code>{&quot;color&quot;: &quot;WHITE&quot;}</code></li><li>服务器端可以把所有的错误异常都映射为 error 模板</li></ul><figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"code"</span> : <span class="number">422</span>,</span><br><span class="line">  <span class="attr">"message"</span> : <span class="string">"Unprocessable Entity"</span>,</span><br><span class="line">  <span class="attr">"errors"</span> : [</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="attr">"field"</span> : <span class="string">"first_name"</span>,</span><br><span class="line">      <span class="attr">"message"</span> : <span class="string">"First name cannot have fancy characters"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="attr">"field"</span> : <span class="string">"password"</span>,</span><br><span class="line">       <span class="attr">"message"</span> : <span class="string">"Password cannot be blank"</span></span><br><span class="line">    &#125;</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>参考：<a href="https://github.com/darcyliu/google-styleguide/blob/master/JSONStyleGuide.md" target="_blank" rel="noopener">Google JSON风格指南</a></p></blockquote><h3 id="分页"><a href="#分页" class="headerlink" title="分页"></a>分页</h3><p>推荐使用下列属性对数据进行分页：</p><ul><li>page 当前页码</li><li>page_size 每页返回多少数据，可以通过参数指定，但是服务器需设置上限</li><li>total_page 总页数</li><li>has_next 是否有下一页</li></ul><p>total_page 与 has_next 可以二选一</p><h2 id="OTHERS"><a href="#OTHERS" class="headerlink" title="OTHERS"></a>OTHERS</h2><h3 id="身份认证"><a href="#身份认证" class="headerlink" title="身份认证"></a>身份认证</h3><ul><li>basic 认证，只有部署 SSL 证书情况下使用，不推荐</li><li>token：header 增加 <code>Authorization: Bearer {token}</code> 或者 <code>x-Token</code> 验证</li><li><a href="http://oauth.net/2/" target="_blank" rel="noopener">OAuth</a></li></ul><h3 id="命名"><a href="#命名" class="headerlink" title="命名"></a>命名</h3><p>URL 以及 HTTP Header 中的关键字推荐使用短横线做连接符，例如:</p><p>不推荐: <code>http://domain.com/api/book_list</code><br>推 荐: <code>http://domain.com/api/book-list</code> </p><p>参数(URL Param 和 JSON Key)推荐使用下划线或驼峰式命名，如：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://domain.com/api/book?book_name=java</span><br><span class="line">http://domain.com/api/book?bookName=java</span><br></pre></td></tr></table></figure><h3 id="时间-amp-经纬度"><a href="#时间-amp-经纬度" class="headerlink" title="时间 &amp; 经纬度"></a>时间 &amp; 经纬度</h3><p>时间格式遵循 <a href="https://www.iso.org/obp/ui/#iso:std:iso:8601:ed-3:v1:en" target="_blank" rel="noopener">ISO 8601</a> 建议的格式: </p><ul><li>日期: <code>2014-07-09</code></li><li>时间: <code>14:31:22+0800</code></li><li>具体时间: <code>2007-11-06T16:34:41Z</code></li><li>持续时间: <code>P1Y3M5DT6H7M30S</code> （表示在一年三个月五天六小时七分三十秒内）</li><li>时间区间: <code>2007-03-01T13:00:00Z/2008-05-11T15:30:00Z</code>、 <code>2007-03-01T13:00:00Z/P1Y2M10DT2H30M</code></li><li>重复时间: <code>R3/2004-05-06T13:00:00+08/P0Y6M5DT3H0M0S</code> （表示从2004年5月6日北京时间下午1点起，在半年零5天3小时内，重复3次）</li></ul><p>时区推荐默认使用 <a href="https://zh.wikipedia.org/wiki/%E5%8D%8F%E8%B0%83%E4%B8%96%E7%95%8C%E6%97%B6" target="_blank" rel="noopener">UTC</a> 时间</p><p>纬度/经度应该使用 <a href="https://zh.wikipedia.org/wiki/ISO_6709" target="_blank" rel="noopener">ISO 6709</a> 建议的格式</p><figure class="highlight"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  // 自由女神像的纬度/经度位置.</span><br><span class="line">  "statueOfLiberty": "+40.6894-074.0447"</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="安全"><a href="#安全" class="headerlink" title="安全"></a>安全</h3><p>使用 SSL，禁用 80 端口访问。</p><h3 id="缓存"><a href="#缓存" class="headerlink" title="缓存"></a>缓存</h3><p>缓存是客户端的一个重要功能，可以有效减少用户流量，比较推荐的方法是客户端实现 <code>Cache-Control</code> 的缓存逻辑，然后可以根据服务器返回来实现对应的缓存策略。</p><p><code>Cache-Control</code> 通常有下面几种取值：</p><ul><li>private: 仅用于缓存用户个人资料，其他用户无法访问，且只有第一次会发送请求，以后直接从缓存读取。</li><li>no-cache: 客户端 request 会携带 <code>Last-Modified</code> 或 <code>Etag</code> 询问服务器资源是否更改，如果未更改，则使用缓存。</li><li>max-age: <code>max-age=60</code> 则表示缓存60秒内有效</li><li>must-revalidate: 除非用户强制刷新，否则使用缓存数据</li></ul><h4 id="Last-Modified"><a href="#Last-Modified" class="headerlink" title="Last-Modified"></a>Last-Modified</h4><p>客户端第一次请求时，服务器端的返回 HTTP 200，同时携带 <code>Last-Modified</code> 标记该资源在服务器端最后被修改的时间</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Last-Modified:Tue, 24 Feb 2009 08:01:04 GMT</span><br></pre></td></tr></table></figure><p>客户端第二次该资源时，向服务器传送 <code>If-Modified-Since</code> 询问该时间之后文件是否有被修改过：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">If-Modified-Since:Tue, 24 Feb 2009 08:01:04 GMT</span><br></pre></td></tr></table></figure><p>如果服务器端的资源没有变化，则自动返回 HTTP 304 状态码，内容为空。当服务器端代码发生改变或者重启服务器时，则重新发出资源，返回和第一次请求时类似。</p><h4 id="Etag-1"><a href="#Etag-1" class="headerlink" title="Etag"></a>Etag</h4><p>首次请求服务器返回资源，header 中包含 <code>Etag</code>，http code 200</p><p>Etag: “qwghu2u3423kjgh234iughjkh”</p><p>客户端请求同一个文件，header 附加 <code>If-None-Match</code> 字段为服务器返回的 Etag</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">If-None-Match:“qwghu2u3423kjgh234iughjkh″</span><br></pre></td></tr></table></figure><p>服务器检查完修改时间和Etag之后，若资源未改变，则返回 http code 304；否则返回 HTTP 200 并附带新的 Etag。</p><h4 id="Expires-or-max-age"><a href="#Expires-or-max-age" class="headerlink" title="Expires or max-age"></a>Expires or max-age</h4><p>服务器也可以在 Response Header 中附加 Expires 字段，表明资源有效期，但是 Expires 存在服务器时间和客户端时间不同步的问题，所以还是推荐使用 max-age。</p><p>max-age 可以和 Etag 或 Last-Modified 配合使用。max-age 有效期内，不访问网络，超出有效期则访问服务器询问资源是否被修改。</p><blockquote><p>参考：<br><a href="https://developers.google.com/web/fundamentals/performance/optimizing-content-efficiency/http-caching?hl=zh-cn" target="_blank" rel="noopener">max-age</a><br><a href="https://blog.toright.com/posts/3414/%E5%88%9D%E6%8E%A2-http-1-1-cache-%E6%A9%9F%E5%88%B6.html" target="_blank" rel="noopener">初探 HTTP 1.1 Cache 機制</a><br><a href="http://blog.csdn.net/eroswang/article/details/8302191" target="_blank" rel="noopener">浏览器缓存详解:expires,cache-control,last-modified,etag详细说明</a></p></blockquote><h3 id="错误码"><a href="#错误码" class="headerlink" title="错误码"></a>错误码</h3><p>服务器目前只有200，500错误。其它错误均在 Response Body 的 code 字段中返回。</p><p>尽管错误码在 Response Body 中，但是我们依然遵循 http status code 对错误码范围的划分，主要考虑就是以后如果使用 Http status code 返回的方式时，目前定义的错误码依然可以使用。</p><p>具体为：</p><ul><li><p>200 段表示请求成功</p><ul><li>200 OK : 请求执行成功并返回相应数据，如 GET 成功</li><li>201 Created : 对象创建成功并返回相应资源数据，如 POST 成功</li><li>202 Accepted : 表示一个请求已经进入后台排队（异步任务）</li><li>204 No Content : 请求执行成功，不返回相应资源数据，如 PATCH ， DELETE 成功</li></ul></li><li><p>300 段资源重定向，重定向的新地址都需要在响应头 Location 中返回</p><ul><li>301 Moved Permanently : 被请求的资源已永久移动到新位置，需 location</li><li>302 Found : 请求的资源临时移动到新位置，需 location</li><li>304 Not Modified : 资源自从上次请求后没有再次发生变化，主要使用场景在于实现数据缓存</li></ul></li><li><p>400 段表示客户端错误</p><ul><li>400 Bad Request : 请求体包含语法错误(请求格式错误)</li><li>401 Unauthorized : 用户没有权限（令牌、用户名、密码错误）</li><li>403 Forbidden : 表示用户验证成功，但是没有该资源权限。</li><li>404 Not Found : 找不到目标资源</li><li>405 Method Not Allowed : 不允许执行目标方法，响应中应该带有 Allow 头，内容为对该资源有效的 HTTP 方法</li><li>406 Not Acceptable : 服务器不支持客户端请求的内容格式，但响应里会包含服务端能够给出的格式的数据，并在 Content-Type 中声明格式名称</li><li>409 Conflict : 请求操作和资源的当前状态存在冲突。主要使用场景在于实现并发控制</li><li>410 Gone : 被请求的资源已被删除，只有在确定了这种情况是永久性的时候才可以使用，否则建议使用 404 Not Found</li><li>422 Unprocessable Entity : 请求格式正确，但是由于含有语义错误(校验错误)，无法响应</li></ul></li><li><p>500 段表示服务器错误</p><ul><li>500 Internal Server Error : 服务器遇到了一个未曾预料的状况，导致了它无法完成对请求的处理。</li><li>501 Not Implemented : 服务器不支持当前请求所需要的某个功能。</li><li>502 Bad Gateway : 作为网关或者代理工作的服务器尝试执行请求时，从上游服务器接收到无效的响应。</li><li>503 Service Unavailable : 由于临时的服务器维护或者过载，服务器当前无法处理请求。这个状况是临时的，并且将在一段时间以后恢复。如果能够预计延迟时间，那么响应中可以包含一个 Retry-After 头用以标明这个延迟时间（内容可以为数字，单位为秒；或者是一个 HTTP 协议指定的时间格式）。如果没有给出这个 Retry-After 信息，那么客户端应当以处理 500 响应的方式处理它。</li></ul></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;该文档参考 REST Web API 设计，但是考虑到目前服务器所使用技术完全使用 REST API 工作量过大。所以做了一些妥协。&lt;/p&gt;
&lt;p&gt;加星号部分可根据需求选择性实现。&lt;/p&gt;
    
    </summary>
    
      <category term="HTTP" scheme="https://honglu.me/categories/HTTP/"/>
    
    
      <category term="iOS" scheme="https://honglu.me/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>Threejs 全景探索</title>
    <link href="https://honglu.me/2016/06/22/Threejs%E5%85%A8%E6%99%AF%E6%8E%A2%E7%B4%A2/"/>
    <id>https://honglu.me/2016/06/22/Threejs全景探索/</id>
    <published>2016-06-22T03:20:00.000Z</published>
    <updated>2018-08-09T03:55:11.795Z</updated>
    
    <content type="html"><![CDATA[<p>前端制作全景使用 krpano 的比较多，这是一套比较成熟的东西了，功能非常强大，包括 <a href="http://720yun.com/" target="_blank" rel="noopener">720云</a> 等网站都在使用，但是缺点在于是需要商用授权的，而且灵活性不高。另一种方案就是使用 ThreeJS 来实现，ThreeJS 是一套 JavaScript 3D 库，全景实际上也是对 3D 场景的展示，使用 ThreeJS 可以实现任意的定制，但是由于是从头实现，且社区组件等不是太完善，所以复杂功能实现的成本相对较高。</p><p>这次只做全景主要就是为了抛弃 krpano，所以从开始就选定了 ThreeJS 方案。关于 3D 场景的基础知识网上有很多教程。这里总结一下这次做全景遇到的一些问题。</p><a id="more"></a><h2 id="全景方案"><a href="#全景方案" class="headerlink" title="全景方案"></a>全景方案</h2><p>ThreeJS 做全景可以使用球模型，也可以使用立方体模型，还可以使用圆柱模型，这个一方面看设备输出，另一方面看自己的需求啦。模型确定之后，三维到二维的投影也有很多种，可以参考『<a href="https://zh.wikipedia.org/wiki/%E5%9C%B0%E5%9B%BE%E6%8A%95%E5%BD%B1" target="_blank" rel="noopener">地图投影</a>』</p><p>我们使用 iPhone 拍摄的全景照片其实就适用于圆柱模型，但是通过柱面投影之后会导致上下面没有图像，所以常规做法是在圆柱上下面贴个大 logo。</p><p>球模型的投影方式也有多种，常用的<a href="https://zh.wikipedia.org/wiki/%E7%90%83%E6%A5%B5%E5%B9%B3%E9%9D%A2%E6%8A%95%E5%BD%B1" title="wikipedia" target="_blank" rel="noopener">球极平面投影</a>(小行星)、<a href="(https://zh.wikipedia.org/wiki/%E9%BA%A5%E5%8D%A1%E6%89%98%E6%8A%95%E5%BD%B1%E6%B3%95)" title="wikipedia">麦卡托投影</a>(世界地图)等。可以通过多拍几张照片，然后使用 PTGui 软件来合成一张使用<a href="(https://zh.wikipedia.org/wiki/%E9%BA%A5%E5%8D%A1%E6%89%98%E6%8A%95%E5%BD%B1%E6%B3%95)" title="wikipedia">麦卡托投影</a>或<a href="https://zh.wikipedia.org/wiki/%E7%90%83%E6%A5%B5%E5%B9%B3%E9%9D%A2%E6%8A%95%E5%BD%B1" title="wikipedia" target="_blank" rel="noopener">球极平面投影</a>的全景图。其原理就是多张图片之间存在较大冗余，然后通过 OpenCV 取特征点进行图像拼接，最终生成全景图。</p><p>立方体模型要求比较高，其需要面与面之间能够完美拼合。其实立方体模型也可以通过球面全景图生成的，其先使用球面全景图生成球模型，然后分别对球模型的六个面生成六张图片。</p><p>我选择的方案是球模型，使用<a href="(https://zh.wikipedia.org/wiki/%E9%BA%A5%E5%8D%A1%E6%89%98%E6%8A%95%E5%BD%B1%E6%B3%95)" title="wikipedia">麦卡托投影</a>，主要原因是我们的设备输出就是这种投影方式的全景图。</p><p>另外，使用立方体投影映射的方式，是可以减小图片资源的大小的，这个我没有具体试过，可以参考<a href="http://yivian.com/2016/01/22/vr330/13051" target="_blank" rel="noopener">这里</a></p><h2 id="threejs-加载-3D-模型"><a href="#threejs-加载-3D-模型" class="headerlink" title="threejs 加载 3D 模型"></a>threejs 加载 3D 模型</h2><p>也许是因为前端本身的性能问题，threeJS 虽然对 dae，fbx 等格式提供相应的 loader 文件，但是经过测试发现支持都不够好，FBX ASCII编码方式是一种不常用的编码格式，dae 格式对动画支持不好。</p><p>另外官方提供的 3DMAX 的导出 json 插件，容错性也不好。</p><p>不管怎样，使用 threejs 自定义的 json 模型还是可以的，只是导出这种格式比较麻烦。</p><h2 id="VR"><a href="#VR" class="headerlink" title="VR"></a>VR</h2><p>浏览器有 <a href="(https://webvr.info/)">WebVR</a> 规范，但是目前除了火狐之外，chrome 和 Safari 都还没有支持，而对于移动端，chrome 和 Safari 应该是两大主流浏览器了，所以在移动端使用 VR，需要引入<a href="https://github.com/borismus/webvr-polyfill" target="_blank" rel="noopener">webvr-polyfill</a>这个 js 库，然后我们可以通过 <a href="https://github.com/mrdoob/three.js/blob/master/examples/js/effects/VREffect.js" target="_blank" rel="noopener">VREffect</a> 和 <a href="https://github.com/mrdoob/three.js/blob/master/examples/js/controls/VRControls.js" target="_blank" rel="noopener">VRControls</a> 这两个组件实现双眼 VR 的功能了。另外，<a href="https://github.com/borismus/webvr-boilerplate" target="_blank" rel="noopener">webvr-boilerplate</a>还提供了一个<a href="https://github.com/borismus/webvr-boilerplate/blob/master/build/webvr-manager.js" target="_blank" rel="noopener">webvr-manager</a>，使用方法可以参考<a href="https://www.sitepoint.com/how-to-build-vr-on-the-web-today/" target="_blank" rel="noopener">这里</a>。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://aotu.io/notes/2016/01/02/3D-panorama/" target="_blank" rel="noopener">使用ThreeJS在浏览器中展示全景图</a><br><a href="https://isux.tencent.com/3d.html" target="_blank" rel="noopener">打造H5里的“3D全景漫游”秘籍</a><br><a href="https://github.com/tmallfe/tmallfe.github.io/issues/21" target="_blank" rel="noopener">3D互动游戏实践</a></p><p>更多关于 threejs 全景方面的使用方法，可以参考我整理的 Wiki：<a href="http://wiki.honglu.me/web/ThreeJS.html" target="_blank" rel="noopener">ThreeJS</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前端制作全景使用 krpano 的比较多，这是一套比较成熟的东西了，功能非常强大，包括 &lt;a href=&quot;http://720yun.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;720云&lt;/a&gt; 等网站都在使用，但是缺点在于是需要商用授权的，而且灵活性不高。另一种方案就是使用 ThreeJS 来实现，ThreeJS 是一套 JavaScript 3D 库，全景实际上也是对 3D 场景的展示，使用 ThreeJS 可以实现任意的定制，但是由于是从头实现，且社区组件等不是太完善，所以复杂功能实现的成本相对较高。&lt;/p&gt;
&lt;p&gt;这次只做全景主要就是为了抛弃 krpano，所以从开始就选定了 ThreeJS 方案。关于 3D 场景的基础知识网上有很多教程。这里总结一下这次做全景遇到的一些问题。&lt;/p&gt;
    
    </summary>
    
      <category term="web" scheme="https://honglu.me/categories/web/"/>
    
    
      <category term="Threejs" scheme="https://honglu.me/tags/Threejs/"/>
    
  </entry>
  
  <entry>
    <title>我的个人 Wiki</title>
    <link href="https://honglu.me/2016/05/30/%E6%88%91%E7%9A%84%E4%B8%AA%E4%BA%BAWiki/"/>
    <id>https://honglu.me/2016/05/30/我的个人Wiki/</id>
    <published>2016-05-30T08:54:00.000Z</published>
    <updated>2018-08-09T03:55:11.823Z</updated>
    
    <content type="html"><![CDATA[<p>之所以要搞这个个人 Wiki 是因为看了一篇文章 <a href="http://blog.jobbole.com/101321/" target="_blank" rel="noopener">《程序员的知识管理》</a> ，里面提到个人 Wiki 的重要性。</p><p>对于我来说，通常一些知识碎片又不值得写篇博客，都随手写完放到 dropbox 里面了，也懒得进行分类处理，导致放的到处都是，用的时候我自己都得翻半天。刚好文章里面提到了 simiki，于是我就搜索了一番，发现这个 markdown 的 Wiki 系统确实足够满足我的日常需求了，于是就抽时间搭了起来。以后会慢慢把之前整理的一些东西搬到这个 Wiki 上面来，方面统一管理。</p><p>另外， <a href="http://blog.jobbole.com/101321/" target="_blank" rel="noopener">《程序员的知识管理》</a> 里面提到的另一篇文章 <a href="http://www.jianshu.com/p/dbdac17eb9ff" target="_blank" rel="noopener">《个人知识管理的方法》</a> 也值得一读~</p><p>Wiki 地址：<a href="http://wiki.honglu.me" target="_blank" rel="noopener">http://wiki.honglu.me</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;之所以要搞这个个人 Wiki 是因为看了一篇文章 &lt;a href=&quot;http://blog.jobbole.com/101321/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;《程序员的知识管理》&lt;/a&gt; ，里面提到个人 Wiki 的重要性。&lt;/p&gt;
&lt;
      
    
    </summary>
    
      <category term="闲聊" scheme="https://honglu.me/categories/%E9%97%B2%E8%81%8A/"/>
    
    
      <category term="闲聊" scheme="https://honglu.me/tags/%E9%97%B2%E8%81%8A/"/>
    
  </entry>
  
  <entry>
    <title>我想念你的多种方式</title>
    <link href="https://honglu.me/2016/04/30/%E6%88%91%E6%83%B3%E5%BF%B5%E4%BD%A0%E7%9A%84%E5%A4%9A%E7%A7%8D%E6%96%B9%E5%BC%8F/"/>
    <id>https://honglu.me/2016/04/30/我想念你的多种方式/</id>
    <published>2016-04-30T02:04:00.000Z</published>
    <updated>2018-08-09T03:55:11.822Z</updated>
    
    <content type="html"><![CDATA[<p>　　比如，我改变一种习惯，从前我不吃胡萝卜，现在我开始吃胡萝卜，这样每次我吃胡萝卜的时候，都想到你；</p><p>　　比如，从现在开始，一年之内，我搭乘公车，都提前一站下来，走路到目的地，这样我可以慢下来，看看周围的人和树，还有店铺，它们沉默不语，我也是，因为我想到了你；</p><p>　　比如，有一个词我永远都不用，每次要用到的时候，我很小心，绕开它，换过一个词，这样我又想到你一次；</p><p>　　比如，每次我到大街上，如果我很高兴，我就大声叫你的名字，你的名字听起来这么普通，所以，我每次叫你，都会有人回过头来看我，以为我叫的是他们，他们也许对我笑，也许露出困惑的表情，我很高兴，我觉得也许有一次，回头看我的人正好是你；</p><p>　　比如，夏天的时候，天气太热了，我把手里的水从头上淋下来，这么凉，我把这个行为叫做你；</p><a id="more"></a><p>　　比如，我喝酒，我从前常常和你一起喝酒，我一口气喝完了整杯酒，我把这个俗气，而又常见的喝酒方式，都宣称是和你喝酒时才用的；</p><p>　　比如，我要去锻炼了，我是这么讨厌锻炼的人，但你叫我去锻炼，我就去锻炼，这样我每次都皱着眉头，想起你一次；</p><p>　　比如，我不怎么提起，也不怎么去你的那个城市了，但正因为如此，我反而常常想起那里，想起你；</p><p>　　比如，在路上，我看见每个陌生人，他们或者高兴，或者悲伤，或者面无表情，我都觉得他们是有理由的，因为他们不认识你；而我自己或者高兴，或者悲伤，或者面无表情，都是有理由的，因为我认识你；这样子随时随地都可能想起你；</p><p>　　比如，我找到那些和我一样认识你的人，他们或者提起你，或者不提起你，但我知道都和你有关，不管我见到几个人，但至少都要想到你一次；</p><p>　　比如，我阅读那些俗气的书，阅读那些高深的书，都有可能想到你，因为你就是这么无孔不入，既阅读俗气的书，又阅读高深的书；</p><p>　　比如，我写小故事给你，因为你要我写小故事给你，我始终都没有写，这次我终于写了，故事里的人和你有一点像，我每看一次，就想到你一次；</p><p>　　比如，《Perfect Day》，这首歌我也很喜欢，你也很喜欢，这个词我也很喜欢，你也很喜欢，但我被你打败了，这个词归你了，这首歌归你了，它变成你喜欢的，我才喜欢了；别人也被你打败了，他们都变成因为你喜欢，所以我才会注意到他们也喜欢；</p><p>　　比如，以后我不能玩捉迷藏游戏，因为每次我都想到你藏起来了，我可能找不到你；但我又会很想要玩捉迷藏游戏，因为到最后，我总归是找得到你的。这样子，无论我玩不玩捉迷藏游戏，我都会想到你；</p><p>　　比如，有一天我无意中找到一本书，那上面有你的签名，又有一天我无意中找到了一张相片，那上面有你，我把它们拿给别人看，说：瞧，我有这么好的东西。那个时候我可能已经有点老，记性也不太好，不过还是想到你一次；</p><p>　　比如，有一天有人问我，认不认识你，记不记得关于你的事情，我回答说：记得。于是我把所有关于你的事情，又想了一遍；</p><p>　　比如，有一天有人问我，认不认识你，记不记得关于你的事情，我回答说：不记得。但是我在心里，把所有关于你的事情，又想了一遍；</p><p>　　比如，你最好马上出现在我面前，嘲笑我，对我哈哈大笑，这样子我会马上原谅你，并且给你看这篇东西，我会同样哈哈大笑，说：我一点都不想念你。不过这样说其实是因为我有点羞涩。</p><p>　　比如。比如。</p><p>　　比如我写下来，这一篇给你，给马骅。我自己又从头看了一遍，觉得可以给你了。给马骅。天已经亮了，从天很黑到天开始亮，有好几个小时，我都想到你了。我的朋友会跟我提到这一篇，提到一次，就又想到你一次。</p><p>　　—— 饭饭《路上有惊慌》</p><!--附：　　喝过的美酒都忘记了，只有那青稞酒忘不了。　　经过的村庄都忘记了，只有那明永村忘不了。　　走过的大河都忘记了，只有那澜沧江忘不了。　　看过的雪山都忘记了，只有那梅里雪山忘不了。　　—— 摘自马骅诗篇《明永歌谣》　　马骅，毕业于复旦大学国际政治系，诗人。2003年3月，离开北京去云南迪庆州德钦县明永村支教，2004年6月20在当地因车祸去世。-->]]></content>
    
    <summary type="html">
    
      &lt;p&gt;　　比如，我改变一种习惯，从前我不吃胡萝卜，现在我开始吃胡萝卜，这样每次我吃胡萝卜的时候，都想到你；&lt;/p&gt;
&lt;p&gt;　　比如，从现在开始，一年之内，我搭乘公车，都提前一站下来，走路到目的地，这样我可以慢下来，看看周围的人和树，还有店铺，它们沉默不语，我也是，因为我想到了你；&lt;/p&gt;
&lt;p&gt;　　比如，有一个词我永远都不用，每次要用到的时候，我很小心，绕开它，换过一个词，这样我又想到你一次；&lt;/p&gt;
&lt;p&gt;　　比如，每次我到大街上，如果我很高兴，我就大声叫你的名字，你的名字听起来这么普通，所以，我每次叫你，都会有人回过头来看我，以为我叫的是他们，他们也许对我笑，也许露出困惑的表情，我很高兴，我觉得也许有一次，回头看我的人正好是你；&lt;/p&gt;
&lt;p&gt;　　比如，夏天的时候，天气太热了，我把手里的水从头上淋下来，这么凉，我把这个行为叫做你；&lt;/p&gt;
    
    </summary>
    
      <category term="闲聊" scheme="https://honglu.me/categories/%E9%97%B2%E8%81%8A/"/>
    
    
      <category term="闲聊" scheme="https://honglu.me/tags/%E9%97%B2%E8%81%8A/"/>
    
  </entry>
  
  <entry>
    <title>《禅与Objective-C编程艺术》笔记</title>
    <link href="https://honglu.me/2016/03/16/%E3%80%8A%E7%A6%85%E4%B8%8EObjective-C%E7%BC%96%E7%A8%8B%E8%89%BA%E6%9C%AF%E3%80%8B%E7%AC%94%E8%AE%B0/"/>
    <id>https://honglu.me/2016/03/16/《禅与Objective-C编程艺术》笔记/</id>
    <published>2016-03-16T09:04:00.000Z</published>
    <updated>2018-08-09T03:55:11.818Z</updated>
    
    <content type="html"><![CDATA[<p>GitBook for <a href="https://github.com/oa414/objc-zen-book-cn" target="_blank" rel="noopener">《禅与 Objective-C 编程艺术》</a> 笔记。</p><h2 id="Golden-Path"><a href="#Golden-Path" class="headerlink" title="Golden Path"></a>Golden Path</h2><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)someMethod &#123;</span><br><span class="line">  <span class="keyword">if</span> (![someOther boolValue]) &#123;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//Do something important</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h2><p>通过参数返回 error 的引用，使用这样的方法时应当检查方法的返回值，而非 error 的引用。</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSError</span> *error = <span class="literal">nil</span>;</span><br><span class="line"><span class="keyword">if</span> (![<span class="keyword">self</span> trySomethingWithError:&amp;error]) &#123;</span><br><span class="line">    <span class="comment">// Handle Error</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>一些苹果的 API 在成功的情况下会对 error 参数（如果它非 NULL）写入垃圾值（garbage values），所以如果检查 error 的值可能导致错误 （甚至崩溃）。</p><h2 id="命名"><a href="#命名" class="headerlink" title="命名"></a>命名</h2><h3 id="常量加类名前缀"><a href="#常量加类名前缀" class="headerlink" title="常量加类名前缀"></a>常量加类名前缀</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="built_in">NSTimeInterval</span> ZOCSignInViewControllerFadeOutAnimationDuration = <span class="number">0.4</span>;</span><br></pre></td></tr></table></figure><h3 id="方法名少用-and"><a href="#方法名少用-and" class="headerlink" title="方法名少用 and"></a>方法名少用 <code>and</code></h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)setExampleText:(<span class="built_in">NSString</span> *)text image:(<span class="built_in">UIImage</span> *)image;</span><br><span class="line">- (<span class="keyword">void</span>)sendAction:(SEL)aSelector to:(<span class="keyword">id</span>)anObject forAllCells:(<span class="built_in">BOOL</span>)flag;</span><br><span class="line">- (<span class="keyword">id</span>)viewWithTag:(<span class="built_in">NSInteger</span>)tag;</span><br><span class="line">- (<span class="keyword">instancetype</span>)initWithWidth:(<span class="built_in">CGFloat</span>)width height:(<span class="built_in">CGFloat</span>)height;</span><br></pre></td></tr></table></figure><h2 id="类"><a href="#类" class="headerlink" title="类"></a>类</h2><p>类名应该以三个大写字母作为前缀（双字母前缀为 Apple 的类预留），说明性的部分放在前缀和父类名的在中间(如：<code>ZOCTimelineViewController</code>)。</p><h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>推荐的代码组织方式是将 dealloc 方法放在实现文件的最前面（直接在 @synthesize 以及 @dynamic 之后），init 应该跟在 dealloc 方法后面。如果有多个初始化方法， 指定初始化方法 (designated initializer) 应该放在最前面，间接初始化方法 (secondary initializer) 跟在后面，这样更有逻辑性。</p><p>指定初始化器: <code>NS_DESIGNATED_INITIALIZER</code></p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ZOC_UNAVAILABLE_INSTEAD(msg) __attribute__((unavailable(<span class="meta-string">"Invoke the designated initializer, use ["</span>msg <span class="meta-string">"]."</span>)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ZOC_UNAVAILABLE(msg) __attribute__((unavailable(msg)))</span></span><br></pre></td></tr></table></figure><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">ZOCNewsViewController</span> : <span class="title">UIViewController</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">instancetype</span>)initWithNews:(ZOCNews *)news <span class="built_in">NS_DESIGNATED_INITIALIZER</span>;</span><br><span class="line">- (<span class="keyword">instancetype</span>)initWithNibName:(<span class="built_in">NSString</span> *)nibNameOrNil bundle:(<span class="built_in">NSBundle</span> *)nibBundleOrNil ZOC_UNAVAILABLE_INSTEAD(<span class="string">"initWithNews:"</span>);</span><br><span class="line">- (<span class="keyword">instancetype</span>)init ZOC_UNAVAILABLE_INSTEAD(<span class="string">"initWithNews:"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">ZOCNewsViewController</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">id</span>)initWithNews:(ZOCNews *)news</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// call to the immediate superclass's designated initializer （调用直接超类的 designated initializer）</span></span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> initWithNibName:<span class="literal">nil</span> bundle:<span class="literal">nil</span>];</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">self</span>) &#123;</span><br><span class="line">        _news = news;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Override the immediate superclass's designated initializer （重载直接父类的  designated initializer）</span></span><br><span class="line">- (<span class="keyword">id</span>)initWithNibName:(<span class="built_in">NSString</span> *)nibNameOrNil bundle:(<span class="built_in">NSBundle</span> *)nibBundleOrNil</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// call the new designated initializer</span></span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> initWithNews:<span class="literal">nil</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><h3 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">readwrite</span>, <span class="keyword">copy</span>) <span class="built_in">NSString</span> *name;</span><br></pre></td></tr></table></figure><ul><li>属性的参数顺序：原子性，读写 和 内存管理</li><li>setter 方法中 KVO 通知(willChangeValueForKey, didChangeValueForKey) 会被自动执行。</li><li>懒加载是 KVO 不友好的，因为其在 getter 中 change value</li><li>永远不要在 init 方法（以及其他初始化方法）里面用 getter 和 setter 方法</li></ul><h3 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h3><ul><li>善用断言</li><li>私有方法不要用 <code>_</code> 前缀，这是 apple 保留前缀</li></ul><h3 id="相等性"><a href="#相等性" class="headerlink" title="相等性"></a>相等性</h3><ul><li>你需要同时实现 isEqual 和 hash 方法。如果两个对象是被 isEqual 认为相等的，它们的 hash 方法需要返回一样的值。但是如果 hash 返回一样的值，并不能确保他们相等。</li><li>用 <code>isEqualTo&lt;#class-name-without-prefix#&gt;:</code> 这样的格式实现一个相等性检查方法。</li></ul><h2 id="Categories"><a href="#Categories" class="headerlink" title="Categories"></a>Categories</h2><ul><li>category 方法前加小写前缀以及下划线，比如 <code>- (id)zoc_myCategoryMethod</code>。</li></ul><h2 id="protocols"><a href="#protocols" class="headerlink" title="protocols"></a>protocols</h2><ul><li>抽象接口</li><li><a href="http://en.wikipedia.org/wiki/Liskov_substitution_principle" target="_blank" rel="noopener">里氏替换原则</a>: 你应该可以取代任意接口（也就是Objective-C里的的”protocol”）实现，而不用改变客户端或者相关实现。</li></ul><h2 id="NSNotification"><a href="#NSNotification" class="headerlink" title="NSNotification"></a>NSNotification</h2><ul><li>用类名前缀作为这个通知名字的前缀。</li><li>用一个 Did/Will 这样的动词以及用 “Notifications” 后缀来命名这个通知。</li></ul><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Foo.h</span></span><br><span class="line"><span class="keyword">extern</span> <span class="built_in">NSString</span> * <span class="keyword">const</span> ZOCFooDidBecomeBarNotification</span><br><span class="line"></span><br><span class="line"><span class="comment">// Foo.m</span></span><br><span class="line"><span class="built_in">NSString</span> * <span class="keyword">const</span> ZOCFooDidBecomeBarNotification = <span class="string">@"ZOCFooDidBecomeBarNotification"</span>;</span><br></pre></td></tr></table></figure><h2 id="代码组织"><a href="#代码组织" class="headerlink" title="代码组织"></a>代码组织</h2><h3 id="利用代码块"><a href="#利用代码块" class="headerlink" title="利用代码块"></a>利用代码块</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSURL</span> *url = (&#123;</span><br><span class="line">    <span class="built_in">NSString</span> *urlString = [<span class="built_in">NSString</span> stringWithFormat:<span class="string">@"%@/%@"</span>, baseURLString, endpoint];</span><br><span class="line">    [<span class="built_in">NSURL</span> URLWithString:urlString];</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><h3 id="善用-Pragma"><a href="#善用-Pragma" class="headerlink" title="善用 Pragma"></a>善用 Pragma</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)dealloc &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line">- (<span class="keyword">instancetype</span>)init &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - View Lifecycle （View 的生命周期）</span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)viewDidLoad &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line">- (<span class="keyword">void</span>)viewWillAppear:(<span class="built_in">BOOL</span>)animated &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line">- (<span class="keyword">void</span>)didReceiveMemoryWarning &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - Getter &amp; Setter （自定义访问器）</span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)setCustomProperty:(<span class="keyword">id</span>)value &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line">- (<span class="keyword">id</span>)customProperty &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - IBActions  </span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">IBAction</span>)submitData:(<span class="keyword">id</span>)sender &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - Public </span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)publicMethod &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - Private</span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)zoc_privateMethod &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - UITableViewDataSource</span></span><br><span class="line"></span><br><span class="line">- (<span class="built_in">UITableViewCell</span> *)tableView:(<span class="built_in">UITableView</span> *)tableView cellForRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath &#123; <span class="comment">/* ... */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - ZOCSuperclass</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ... 重载来自 ZOCSuperclass 的方法</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#pragma mark - NSObject</span></span><br><span class="line"></span><br><span class="line">- (<span class="built_in">NSString</span> *)description &#123; <span class="comment">/* ... */</span> &#125;</span><br></pre></td></tr></table></figure><h3 id="明确编译器警告和错误"><a href="#明确编译器警告和错误" class="headerlink" title="明确编译器警告和错误"></a>明确编译器警告和错误</h3><figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">error</span> Whoa, buddy, you need to check for zero here!</span></span><br><span class="line">#warning Dude, don't compare floating point numbers like this!</span><br></pre></td></tr></table></figure><h3 id="公开方法属性要有注释"><a href="#公开方法属性要有注释" class="headerlink" title="公开方法属性要有注释"></a>公开方法属性要有注释</h3><h2 id="对象通讯"><a href="#对象通讯" class="headerlink" title="对象通讯"></a>对象通讯</h2><h3 id="block"><a href="#block" class="headerlink" title="block"></a>block</h3><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)downloadObjectsAtPath:(<span class="built_in">NSString</span> *)path</span><br><span class="line">                   completion:(<span class="keyword">void</span>(^)(<span class="built_in">NSArray</span> *objects, <span class="built_in">NSError</span> *error))completion &#123;</span><br><span class="line">    <span class="keyword">if</span> (objects) &#123;</span><br><span class="line">        <span class="comment">// do something with the data</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// some error occurred, 'error' variable should not be nil by contract</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>若 objects 不为 nil，则 error 必须为 nil</li><li>若 objects 为 nil，则 error 必须不为 nil</li></ul><p>注意避免 block 可能引起的循环引用问题，采用<a href="https://github.com/jspahrsummers/libextobjc/blob/master/extobjc/EXTScope.h" target="_blank" rel="noopener">@weakify/@strongify</a></p><h3 id="委托和数据源"><a href="#委托和数据源" class="headerlink" title="委托和数据源"></a>委托和数据源</h3><ul><li>代理方法必须以调用者(即委托者)作为第一个参数</li><li>避免双重引用</li><li>考虑用多重委托代替 NSNotifications。<a href="https://github.com/lukabernardi/LBDelegateMatrioska" target="_blank" rel="noopener">LBDelegateMatrioska</a></li></ul><h2 id="面向切面编程"><a href="#面向切面编程" class="headerlink" title="面向切面编程"></a>面向切面编程</h2><p>Aspect Oriented Programming (AOP，面向切面编程)，在 Objective-C 的世界里，这意味着使用运行时的特性来为指定的方法追加切面 。切面所附加的行为可以是这样的：</p><ul><li>在类的特定方法调用前运行特定的代码</li><li>在类的特定方法调用后运行特定的代码</li><li>增加代码来替代原来的类的方法的实现</li></ul><p><a href="https://github.com/steipete/Aspects" target="_blank" rel="noopener">Aspects</a> 库</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;GitBook for &lt;a href=&quot;https://github.com/oa414/objc-zen-book-cn&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;《禅与 Objective-C 编程艺术》&lt;/a&gt; 笔记。&lt;/p&gt;
&lt;h2 id=&quot;Golden-Path&quot;&gt;&lt;a href=&quot;#Golden-Path&quot; class=&quot;headerlink&quot; title=&quot;Golden Path&quot;&gt;&lt;/a&gt;Golden Path&lt;/h2&gt;&lt;figure class=&quot;highlight objc&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;- (&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt;)someMethod &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (![someOther boolValue]) &amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;      &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt;;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &lt;span class=&quot;comment&quot;&gt;//Do something important&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="iOS" scheme="https://honglu.me/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>ProtocalKit 解析</title>
    <link href="https://honglu.me/2016/03/10/ProtocolKit%E8%A7%A3%E6%9E%90/"/>
    <id>https://honglu.me/2016/03/10/ProtocolKit解析/</id>
    <published>2016-03-10T07:03:00.000Z</published>
    <updated>2018-08-09T03:55:11.790Z</updated>
    
    <content type="html"><![CDATA[<p>在 GitHub 上面看到的这个项目，类可以通过 protocol 继承其方法。感觉很好奇，于是就研究了一下其代码，代码不多，但是思路很好~</p><p>项目地址: <a href="https://github.com/forkingdog/ProtocolKit" target="_blank" rel="noopener">ProtocalKit</a></p><p>使用方式见: <a href="https://github.com/forkingdog/ProtocolKit/blob/master/README.md" target="_blank" rel="noopener">README.md</a></p><a id="more"></a><h2 id="作者思路"><a href="#作者思路" class="headerlink" title="作者思路"></a>作者思路</h2><p>作者通过一个关键字 <code>@defs(Forkable)</code>，创建了一个临时类，临时类实现了 Forkable 这个协议，所以 fork 和 github 的实现代码其实是在这个临时类里面的。</p><p>然后作者定义了一个结构体：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> &#123;</span><br><span class="line">    Protocol *__<span class="keyword">unsafe_unretained</span> protocol;</span><br><span class="line">    Method *instanceMethods;</span><br><span class="line">    <span class="keyword">unsigned</span> instanceMethodCount;</span><br><span class="line">    Method *classMethods;</span><br><span class="line">    <span class="keyword">unsigned</span> classMethodCount;</span><br><span class="line">&#125; PKExtendedProtocol;</span><br></pre></td></tr></table></figure><p>在临时类load时，把这个类里面的所有方法加到Forkable这个协议对应的结构体的方法列表里面。</p><p>然后作者建了一个结构体数组</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> PKExtendedProtocol *allExtendedProtocols = <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure><p>用来存放所有的 protocol，以及其实现方法。</p><p>以上工作完成之后 allExtendedProtocols 就初始化完成了，所有的 protocol 以及其 Method 全部存放在这个数组里面。</p><p>接下来，把 allExtendedProtocols 里面的所有方法，加入到实现这个 protocol 的类的方法列表里面去，实例方法加入到本类的 objc_method_list 里面，类方法就加入到 MetaClass 的 objc_method_list 里面去。如果类本身实现了协议方法，就不会加入。</p><p>这样就间接的把临时类里面的 protocol 实现方法，加入到实现了这个协议的类的方法列表里面去了。</p><p>第一步生成 allExtendedProtocols 数组，作者是在 load 方法里面进行的，也就是说在执行之前，引入这个类的时候就会生成 allExtendedProtocols 数组。</p><p>第二步把 allExtendedProtocols 里面的方法加入到实现协议的类里面，作者通过 <code>__attribute__((constructor))</code> 这个属性，在 main 函数被调用之前调用。然后作者通过一个互斥锁来保证这两步的先后顺序，即在第二步执行前 allExtendedProtocols 数组已经初始化完成。</p><h2 id="注释代码"><a href="#注释代码" class="headerlink" title="注释代码"></a>注释代码</h2><p>添加注释后的代码已上传 GitHub 请前往下载阅读</p><p>下载地址： <a href="https://github.com/pljhonglu/ProtocolKit" target="_blank" rel="noopener">ProtocalKit-注释版</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在 GitHub 上面看到的这个项目，类可以通过 protocol 继承其方法。感觉很好奇，于是就研究了一下其代码，代码不多，但是思路很好~&lt;/p&gt;
&lt;p&gt;项目地址: &lt;a href=&quot;https://github.com/forkingdog/ProtocolKit&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;ProtocalKit&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;使用方式见: &lt;a href=&quot;https://github.com/forkingdog/ProtocolKit/blob/master/README.md&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;README.md&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="protocol" scheme="https://honglu.me/tags/protocol/"/>
    
  </entry>
  
  <entry>
    <title>Cocoa中常用的宏定义</title>
    <link href="https://honglu.me/2016/03/04/Cocoa%E4%B8%AD%E5%B8%B8%E7%94%A8%E7%9A%84%E5%AE%8F%E5%AE%9A%E4%B9%89/"/>
    <id>https://honglu.me/2016/03/04/Cocoa中常用的宏定义/</id>
    <published>2016-03-04T09:23:00.000Z</published>
    <updated>2019-06-12T06:17:51.236Z</updated>
    
    <content type="html"><![CDATA[<p>我们在 Cocoa 的头文件中常会看到很多宏定义，这里列举几个可能会用到的</p><h2 id="NS-AVAILABLE"><a href="#NS-AVAILABLE" class="headerlink" title="NS_AVAILABLE"></a>NS_AVAILABLE</h2><p>这个宏定义常用于 API 版本控制，表明函数/属性的使用版本，是否废弃等情况，用法如下：</p><p>该方法在 iOS 4.0 中引入</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+ (<span class="keyword">void</span>)animateWithDuration:(<span class="built_in">NSTimeInterval</span>)duration </span><br><span class="line">                 animations:(<span class="keyword">void</span> (^)(<span class="keyword">void</span>))animations </span><br><span class="line">                 completion:(<span class="keyword">void</span> (^ __<span class="keyword">nullable</span>)(<span class="built_in">BOOL</span> finished))completion <span class="built_in">NS_AVAILABLE_IOS</span>(<span class="number">4</span>_0);</span><br></pre></td></tr></table></figure><a id="more"></a><p>该方法在 OS X 10.6 和 iOS 4.0 中引入</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)enumerateObjectsUsingBlock:(<span class="keyword">void</span> (^)(ObjectType obj, <span class="built_in">NSUInteger</span> idx, <span class="built_in">BOOL</span> *stop))block <span class="built_in">NS_AVAILABLE</span>(<span class="number">10</span>_6, <span class="number">4</span>_0);</span><br></pre></td></tr></table></figure><p>该方法在 iOS 2.0 中引入在 iOS 6.0 后废弃</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="built_in">CGSize</span>)sizeWithFont:(<span class="built_in">UIFont</span> *)font <span class="built_in">NS_DEPRECATED_IOS</span>(<span class="number">2</span>_0, <span class="number">7</span>_0, <span class="string">"Use -sizeWithAttributes:"</span>) __TVOS_PROHIBITED;</span><br></pre></td></tr></table></figure><p>该方法在 OS X 10.0 以及 iOS 2.0 引入，在 OS X 10.6 以及 iOS 4.0 后废弃</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)removeObjectsFromIndices:(<span class="built_in">NSUInteger</span> *)indices </span><br><span class="line">                      numIndices:(<span class="built_in">NSUInteger</span>)cnt <span class="built_in">NS_DEPRECATED</span>(<span class="number">10</span>_0, <span class="number">10</span>_6, <span class="number">2</span>_0, <span class="number">4</span>_0);</span><br></pre></td></tr></table></figure><h2 id="NS-ASSUME-NONNULL"><a href="#NS-ASSUME-NONNULL" class="headerlink" title="NS_ASSUME_NONNULL"></a>NS_ASSUME_NONNULL</h2><p>这个属性是 objective-c 3.0中引入的新功能，应该是为了和 swift 做兼容的。因为 swift 是有严格的类型检查的，而在objc一向是动态性很强的语言，所以为了和 swift 做兼容，也引入了一些类型检查相关的属性，这就是其中一个</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Father</span> : <span class="title">NSObject</span></span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">copy</span>) <span class="built_in">NSString</span> *name;</span><br><span class="line">+ (<span class="keyword">instancetype</span>)fatherWithName:(<span class="built_in">NSString</span> *)name age:(<span class="built_in">NSInteger</span>)age;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure><p>使用这个宏把整个类包起来，表明类中所有指针类型都不能显示地置为 nil。</p><p>比如</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里会给出警告，因为这句代码显示地给 NS_ASSUME_NONNULL_BEGIN 范围内的指针变量赋值为 nil</span></span><br><span class="line">Father *father = [Father fatherWithName:<span class="literal">nil</span> age:<span class="number">10</span>];</span><br><span class="line"><span class="comment">// 但是如果这么写，xCode 不会给出警告：</span></span><br><span class="line"><span class="built_in">NSString</span> *name = <span class="literal">nil</span>;</span><br><span class="line">Father *father = [Father fatherWithName:name age:<span class="number">10</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 同理，对 NS_ASSUME_NONNULL_BEGIN 范围内的属性显示赋值为 nil，也会有警告：</span></span><br><span class="line">father.name = <span class="literal">nil</span>;</span><br><span class="line"><span class="comment">// 这么写就没有警告：</span></span><br><span class="line"><span class="built_in">NSString</span> *name = <span class="literal">nil</span>;</span><br><span class="line">father.name = name;</span><br></pre></td></tr></table></figure><p>可见，NS_ASSUME_NONNULL_BEGIN 只对 <strong>显式赋值nil</strong> 这种情况编译器会给出警告，所以为了安全起见，代码中依然要对 nil 进行安全判断。</p><p>我们通常把这个宏和 nullable 一起使用，比如 AFNetworking 里面的用法</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">AFHTTPSessionManager</span> : <span class="title">AFURLSessionManager</span> &lt;<span class="title">NSSecureCoding</span>, <span class="title">NSCopying</span>&gt;</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">readonly</span>, <span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">nullable</span>) <span class="built_in">NSURL</span> *baseURL;</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">nullable</span> <span class="built_in">NSURLSessionDataTask</span> *)GET:(<span class="built_in">NSString</span> *)URLString</span><br><span class="line">                            parameters:(<span class="keyword">nullable</span> <span class="keyword">id</span>)parameters</span><br><span class="line">                              progress:(<span class="keyword">nullable</span> <span class="keyword">void</span> (^)(<span class="built_in">NSProgress</span> *downloadProgress)) downloadProgress</span><br><span class="line">                               success:(<span class="keyword">nullable</span> <span class="keyword">void</span> (^)(<span class="built_in">NSURLSessionDataTask</span> *task, <span class="keyword">id</span> _Nullable responseObject))success</span><br><span class="line">                               failure:(<span class="keyword">nullable</span> <span class="keyword">void</span> (^)(<span class="built_in">NSURLSessionDataTask</span> * _Nullable task, <span class="built_in">NSError</span> *error))failure;</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure><h2 id="深入挖掘"><a href="#深入挖掘" class="headerlink" title="深入挖掘"></a>深入挖掘</h2><p>我们深入AVAILABLE相关宏内部，发现其使用了 <code>__attribute__</code> 这个编译命令，先看 NS_AVAILABLE 的相关定义</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NS_AVAILABLE(_mac, _ios) CF_AVAILABLE(_mac, _ios)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NS_AVAILABLE_MAC(_mac) CF_AVAILABLE_MAC(_mac)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NS_AVAILABLE_IOS(_ios) CF_AVAILABLE_IOS(_ios)</span></span><br></pre></td></tr></table></figure><p>在 iOS 平台上，CF_AVAILABLE 是这样定义的：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CF_AVAILABLE(_mac, _ios) __attribute__((availability(ios,introduced=_ios)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CF_AVAILABLE_MAC(_mac) __attribute__((availability(ios,unavailable)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CF_AVAILABLE_IOS(_ios) __attribute__((availability(ios,introduced=_ios)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CF_DEPRECATED(_macIntro, _macDep, _iosIntro, _iosDep, ...) __attribute__((availability(ios,introduced=_iosIntro,deprecated=_iosDep,message=<span class="meta-string">""</span> __VA_ARGS__)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CF_DEPRECATED_MAC(_macIntro, _macDep, ...) __attribute__((availability(ios,unavailable)))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CF_DEPRECATED_IOS(_iosIntro, _iosDep, ...) __attribute__((availability(ios,introduced=_iosIntro,deprecated=_iosDep,message=<span class="meta-string">""</span> __VA_ARGS__)))</span></span><br></pre></td></tr></table></figure><p>下面展开一个方法</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+ (<span class="keyword">void</span>)animateWithDuration:(<span class="built_in">NSTimeInterval</span>)duration </span><br><span class="line">                 animations:(<span class="keyword">void</span> (^)(<span class="keyword">void</span>))animations </span><br><span class="line">                 completion:(<span class="keyword">void</span> (^ __<span class="keyword">nullable</span>)(<span class="built_in">BOOL</span> finished))completion <span class="built_in">NS_AVAILABLE_IOS</span>(<span class="number">4</span>_0);</span><br></pre></td></tr></table></figure><p>展开后</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+ (<span class="keyword">void</span>)animateWithDuration:(<span class="built_in">NSTimeInterval</span>)duration </span><br><span class="line">                 animations:(<span class="keyword">void</span> (^)(<span class="keyword">void</span>))animations </span><br><span class="line">                 completion:(<span class="keyword">void</span> (^ __<span class="keyword">nullable</span>)(<span class="built_in">BOOL</span> finished))completion __attribute__((availability(ios,introduced=<span class="number">4</span>_0)));</span><br></pre></td></tr></table></figure><h2 id="attribute"><a href="#attribute" class="headerlink" title="__attribute__"></a><code>__attribute__</code></h2><p>关键字 <code>__attribute__</code> 是一个编译命令，它可以在变量、函数、类型定义时提供一些「属性」，用来增加一些特殊的用法。这篇文章着重介绍下函数属性和变量属性。</p><h3 id="函数属性"><a href="#函数属性" class="headerlink" title="函数属性"></a>函数属性</h3><h4 id="availability"><a href="#availability" class="headerlink" title="availability"></a>availability</h4><p>availability 遍布于众多的 Cocoa 头文件中，提供对「方法调用」的「控制力度」。</p><p>上面已写过这里不再赘述。</p><h4 id="objc-requires-super-（OC-方法）"><a href="#objc-requires-super-（OC-方法）" class="headerlink" title="objc_requires_super （OC 方法）"></a>objc_requires_super （OC 方法）</h4><p>父类方法声明时，加上：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)fatherMethod __attribute__((objc_requires_super));</span><br></pre></td></tr></table></figure><p>表示子类重写该方法时，一定要调用 super 函数，否则 xCode 给出警告。</p><h4 id="枚举中的-attribute"><a href="#枚举中的-attribute" class="headerlink" title="枚举中的 attribute"></a>枚举中的 attribute</h4><p>枚举中可能有些过时的，可以加上 deprecated 来注明，如果用户使用该值，xCode 会给出warning。</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="built_in">NS_ENUM</span>(<span class="built_in">NSInteger</span>, MyEnum) &#123;</span><br><span class="line">    MyEnumA,</span><br><span class="line">    MyEnumB,</span><br><span class="line">    MyEnumC __attribute__((deprecated)),  <span class="comment">// 标明 MyEnumC 不建议使用</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h4 id="sentinel-（OC-方法、C-函数）"><a href="#sentinel-（OC-方法、C-函数）" class="headerlink" title="sentinel （OC 方法、C 函数）"></a>sentinel （OC 方法、C 函数）</h4><p>明确变参函数需要 nil 参数作为分界</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)endWithNil:(<span class="keyword">nonnull</span> <span class="keyword">id</span>)first, ... __attribute__((sentinel));</span><br></pre></td></tr></table></figure><p>输入代码会自动在后面补一个 nil：<br><img src="http://blog.qiniu.lololo.store/2016-03-04-Cocoa中常用的宏定义-1.jpg" alt="Cocoa中常用的宏定义"></p><h4 id="warn-unused-result-（OC-方法、C-函数）"><a href="#warn-unused-result-（OC-方法、C-函数）" class="headerlink" title="warn_unused_result （OC 方法、C 函数）"></a>warn_unused_result （OC 方法、C 函数）</h4><p>表示用户一定要使用函数的返回值，否则给出 warning。</p><p>比如定义一个函数，并用 warn_unused_result 修饰：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">int</span>)mustCheckReturnValue __attribute__((warn_unused_result)) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果这样调用，xCode 会给出警告：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">[<span class="keyword">self</span> mustCheckReturnValue]; <span class="comment">// Xode 给出警告：Ignoring return value of function declared with warn_unused_result attribute</span></span><br></pre></td></tr></table></figure><p>提示你，此函数的返回值很重要，你应该加以关注：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"%d"</span>, [<span class="keyword">self</span> mustCheckReturnValue]); <span class="comment">// 这里简单打印下，也算是「使用」了返回值:)，警告消失</span></span><br></pre></td></tr></table></figure><h4 id="nonnull-（OC-方法、C-函数）"><a href="#nonnull-（OC-方法、C-函数）" class="headerlink" title="nonnull （OC 方法、C 函数）"></a>nonnull （OC 方法、C 函数）</h4><p>这个上面同样说过，不再赘述</p><h3 id="变量属性"><a href="#变量属性" class="headerlink" title="变量属性"></a>变量属性</h3><h4 id="cleanup"><a href="#cleanup" class="headerlink" title="cleanup"></a>cleanup</h4><p>表示在变量作用域即将结束时，自动执行一个指定的方法。</p><p>举个例子：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 注意这里的参数必须为「变量的地址」的类型</span></span><br><span class="line"><span class="keyword">void</span> cleanupInt(<span class="keyword">int</span> *ip) &#123;</span><br><span class="line">    <span class="comment">// 通过指针依然可以访问变量</span></span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"%d,"</span>, *ip);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)viewDidLoad &#123;</span><br><span class="line">    [<span class="keyword">super</span> viewDidLoad];</span><br><span class="line">    <span class="comment">// Do any additional setup after loading the view, typically from a nib.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> i __attribute__((cleanup(cleanupInt))) = <span class="number">10</span>;</span><br><span class="line">    <span class="comment">// 这里定义一个整型值，加上cleanup属性，并且填入上面的函数 cleanupInt。</span></span><br><span class="line">    <span class="comment">// 在 i 的作用域——也就是 viewDidLoad 函数——即将结束时，自动执行 cleanupInt 函数。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然，除了纯量类型，cleanup 也可以修饰 OC 对象：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> stringCleanUp(<span class="built_in">NSString</span> **pString) &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"%@"</span>, *pString);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)viewDidLoad &#123;</span><br><span class="line">    [<span class="keyword">super</span> viewDidLoad];</span><br><span class="line"></span><br><span class="line">    __autoreleasing <span class="built_in">NSString</span> *string __attribute__((cleanup(stringCleanUp))) = <span class="string">@"end"</span>;</span><br><span class="line">    <span class="comment">// 对于指向objc对象的指针，默认加上 __autoreleasing 修饰，造成类型不匹配，这里强转一下</span></span><br><span class="line">    <span class="comment">// 在 string 的作用域——也就是 viewDidLoad 函数——即将结束时，自动执行 stringCleanUp 函数。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果修饰 block：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> blockCleanUp(<span class="keyword">void</span> (^*block)()) &#123;</span><br><span class="line">    (*block)();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__autoreleasing <span class="keyword">void</span> (^block)() __attribute__((cleanup(blockCleanUp))) = ^&#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"end"</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>上述代码直接在 blockCleanUp函数里调用这个block，也就是说，某些我们想做的事情，可以不必依赖于 cleanup 函数，而在 block 里面直接写出来就好。比如某些成对的操作：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UIGraphicsBeginImageContext</span>(size);</span><br><span class="line"><span class="comment">// some code</span></span><br><span class="line"><span class="built_in">UIGraphicsEndImageContext</span>(); <span class="comment">// 中间有许多代码，导致这句忘了写怎么办？</span></span><br></pre></td></tr></table></figure><p>可以考虑写成：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UIGraphicsBeginImageContext</span>(size);</span><br><span class="line">__autoreleasing <span class="keyword">void</span> (^block)() __attribute__((cleanup(blockCleanUp)))= ^&#123;</span><br><span class="line">    <span class="built_in">UIGraphicsEndImageContext</span>(); </span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// some code</span></span><br></pre></td></tr></table></figure><p>像这样子把成对的操作用这种方式写在一起，防止遗忘，把具体操作移到下方。使用时要一定注意自己的业务场景以及变量的作用域。</p><h4 id="objc-runtime-name"><a href="#objc-runtime-name" class="headerlink" title="objc_runtime_name"></a>objc_runtime_name</h4><p>修改类在 runtime 中的名称</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">__attribute__((objc_runtime_name(<span class="string">"NewFather"</span>)))</span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">Father</span> : <span class="title">NSObject</span></span></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>之后 Father 类在 runtime 中真实名称就变成了 NewFather，通过：</p><p>Class class1 = NSClassFromString(@”Father”); 是获取不到类的，必须要通过：</p><p>Class class1 = NSClassFromString(@”NewFather”); 才可以。</p><p>而且通过：Father *fa = [[Father alloc] init]; 创建一个实例，实例的 isa 为 NewFather。</p><h4 id="unused"><a href="#unused" class="headerlink" title="unused"></a>unused</h4><p>明确变量即使不使用，也不会产生 warning。平时如果这么定义：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSInteger</span> i = <span class="number">10</span>;</span><br></pre></td></tr></table></figure><p>xCode会产生警告，因为 i 以后没有使用过。</p><p>这么写可以去除这个警告：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSInteger</span> i __attribute__((unused)) = <span class="number">10</span>;</span><br></pre></td></tr></table></figure><h4 id="deprecated"><a href="#deprecated" class="headerlink" title="deprecated"></a>deprecated</h4><p>表示变量已经不建议使用。</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">copy</span>) <span class="built_in">NSString</span> *str __attribute__ ((deprecated));</span><br></pre></td></tr></table></figure><p>访问 str 时会给出警告。</p><h3 id="attribute-到底对谁有用"><a href="#attribute-到底对谁有用" class="headerlink" title="__attribute__ 到底对谁有用"></a><code>__attribute__</code> 到底对谁有用</h3><p><code>__attribute__</code> 是在编译阶段起作用的，给函数、变量提供了更多的错误检查、版本控制等能力。其实平时代码中用的并不多，但是如果你在项目中负责公共组件的维护，有些还是很有用的，比如，当接口更新时，用 availability 去标明接口的使用版本、是否废弃；用 objc_requires_super 来要求子类需要调用父类方法等等。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="http://ks.netease.com/blog?id=3780" target="_blank" rel="noopener">在系统头文件中总是看到奇奇怪怪的宏，它们究竟是什么意思？</a><br><a href="http://blog.sunnyxx.com/2014/03/15/objc_arc_secret/" target="_blank" rel="noopener">objc arc的简单探索</a><br><a href="https://gcc.gnu.org/onlinedocs/gcc/Common-Function-Attributes.html#Common-Function-Attributes" target="_blank" rel="noopener">Common-Function-Attributes</a><br><a href="http://blog.sunnyxx.com/2014/09/15/objc-attribute-cleanup/" target="_blank" rel="noopener">黑魔法<strong>attribute</strong>((cleanup))</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;我们在 Cocoa 的头文件中常会看到很多宏定义，这里列举几个可能会用到的&lt;/p&gt;
&lt;h2 id=&quot;NS-AVAILABLE&quot;&gt;&lt;a href=&quot;#NS-AVAILABLE&quot; class=&quot;headerlink&quot; title=&quot;NS_AVAILABLE&quot;&gt;&lt;/a&gt;NS_AVAILABLE&lt;/h2&gt;&lt;p&gt;这个宏定义常用于 API 版本控制，表明函数/属性的使用版本，是否废弃等情况，用法如下：&lt;/p&gt;
&lt;p&gt;该方法在 iOS 4.0 中引入&lt;/p&gt;
&lt;figure class=&quot;highlight objc&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;+ (&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt;)animateWithDuration:(&lt;span class=&quot;built_in&quot;&gt;NSTimeInterval&lt;/span&gt;)duration &lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;                 animations:(&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt; (^)(&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt;))animations &lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;                 completion:(&lt;span class=&quot;keyword&quot;&gt;void&lt;/span&gt; (^ __&lt;span class=&quot;keyword&quot;&gt;nullable&lt;/span&gt;)(&lt;span class=&quot;built_in&quot;&gt;BOOL&lt;/span&gt; finished))completion &lt;span class=&quot;built_in&quot;&gt;NS_AVAILABLE_IOS&lt;/span&gt;(&lt;span class=&quot;number&quot;&gt;4&lt;/span&gt;_0);&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="宏" scheme="https://honglu.me/tags/%E5%AE%8F/"/>
    
  </entry>
  
  <entry>
    <title>如何让自定义 UIView 支持 XIB</title>
    <link href="https://honglu.me/2016/02/25/%E5%A6%82%E4%BD%95%E8%AE%A9%E8%87%AA%E5%AE%9A%E4%B9%89UIView%E6%94%AF%E6%8C%81XIB/"/>
    <id>https://honglu.me/2016/02/25/如何让自定义UIView支持XIB/</id>
    <published>2016-02-25T08:15:00.000Z</published>
    <updated>2019-06-12T06:17:51.234Z</updated>
    
    <content type="html"><![CDATA[<p>当我们拖拽一个 view 到 XIB 上，然后设置 view 属性的时候有没有想到让自己定义的 view 也支持通过 XIB 设置属性，甚至还能实时渲染。</p><p>过去相关接口是不开放的，但是从 XCode6 开始，我们也可以定义像系统控件一样支持在 XIB 视图下设置属性并能实时渲染的控件了。</p><a id="more"></a><h2 id="IBInspectable-XIB-设置属性"><a href="#IBInspectable-XIB-设置属性" class="headerlink" title="@IBInspectable XIB 设置属性"></a>@IBInspectable XIB 设置属性</h2><p>在属性前加上 IBInspectable，就可以再 XIB 中设置这个属性的值了。</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">UIView</span> (<span class="title">CornerRadius</span>)</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>)IBInspectable <span class="built_in">CGFloat</span> cornerRadius;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">UIView</span> (<span class="title">CornerRadius</span>)</span></span><br><span class="line">- (<span class="keyword">void</span>)setCornerRadius:(<span class="built_in">CGFloat</span>)cornerRadius&#123;</span><br><span class="line">    <span class="keyword">self</span>.layer.cornerRadius = cornerRadius;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">CGFloat</span>)cornerRadius&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>.layer.cornerRadius;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p><img src="http://blog.qiniu.lololo.store/2016-03-04-如何让自定义 UIView 支持 XIB-1.png" alt="如何让自定义 UIView 支持 XIB-1"></p><h2 id="IBDesignable-XIB-实时渲染"><a href="#IBDesignable-XIB-实时渲染" class="headerlink" title="@IBDesignable XIB 实时渲染"></a>@IBDesignable XIB 实时渲染</h2><p>只需要在类前加上 <code>IB_DESIGNABLE</code> 即表示该类支持实时渲染</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">IB_DESIGNABLE</span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">MyView</span> : <span class="title">UIView</span></span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>)IBInspectable <span class="built_in">CGFloat</span> cornerRadius;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>)IBInspectable <span class="built_in">CGFloat</span> borderWidth;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>)IBInspectable <span class="built_in">UIColor</span> *borderColor;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>)IBInspectable <span class="built_in">NSString</span> *text;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>)IBInspectable <span class="built_in">UIColor</span> *textColor;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>)IBInspectable <span class="built_in">NSUInteger</span> textSize;<span class="comment">// 这里使用 textSize 是因为 IBInspectable 暂时还不支持 UIFont</span></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">MyView</span></span></span><br><span class="line">- (<span class="keyword">void</span>)drawRect:(<span class="built_in">CGRect</span>)rect &#123;</span><br><span class="line">    [<span class="keyword">super</span> drawRect:rect];</span><br><span class="line">    <span class="comment">// Drawing code</span></span><br><span class="line">    <span class="built_in">UIFont</span> *font = [<span class="built_in">UIFont</span> systemFontOfSize:_textSize];</span><br><span class="line">    <span class="built_in">NSMutableParagraphStyle</span> *style = [[<span class="built_in">NSParagraphStyle</span> defaultParagraphStyle] mutableCopy];</span><br><span class="line">    style.alignment = <span class="built_in">NSTextAlignmentCenter</span>;</span><br><span class="line">    <span class="built_in">NSDictionary</span> *dict = @&#123;<span class="built_in">NSFontAttributeName</span>:font,</span><br><span class="line">                           <span class="built_in">NSForegroundColorAttributeName</span>:_textColor,</span><br><span class="line">                           <span class="built_in">NSParagraphStyleAttributeName</span>:style&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">CGRect</span> contextRect = <span class="keyword">self</span>.bounds;</span><br><span class="line">    <span class="built_in">CGSize</span> size = [_text sizeWithAttributes:dict];</span><br><span class="line">    <span class="keyword">float</span> x_pos = (contextRect.size.width - size.width) / <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">float</span> y_pos = (contextRect.size.height - size.height) /<span class="number">2</span>;</span><br><span class="line">    [_text drawAtPoint:<span class="built_in">CGPointMake</span>(<span class="keyword">self</span>.bounds.origin.x + x_pos, <span class="keyword">self</span>.bounds.origin.y + y_pos) withAttributes:dict];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)setCornerRadius:(<span class="built_in">CGFloat</span>)cornerRadius&#123;</span><br><span class="line">    _cornerRadius = cornerRadius;</span><br><span class="line">    <span class="keyword">self</span>.layer.cornerRadius = cornerRadius;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)setBorderWidth:(<span class="built_in">CGFloat</span>)borderWidth&#123;</span><br><span class="line">    _borderWidth = borderWidth;</span><br><span class="line">    <span class="keyword">self</span>.layer.borderWidth = borderWidth;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)setBorderColor:(<span class="built_in">UIColor</span> *)borderColor&#123;</span><br><span class="line">    _borderColor = borderColor;</span><br><span class="line">    <span class="keyword">self</span>.layer.borderColor = borderColor.CGColor;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p><img src="http://blog.qiniu.lololo.store/2016-03-04-如何让自定义 UIView 支持 XIB-2.png" alt="如何让自定义 UIView 支持 XIB-2"></p><p>让 XIB 实时渲染的方式有很多，可以直接重写 setter 方法，在 setter 里面设置实时渲染；另一个是可以重写 <code>prepareForInterfaceBuilder</code> 方法，该方法只在用于 XIB 渲染的时候调用；另外还可以重写 <code>drawRect:</code> 方法。</p><ul><li>prepareForInterfaceBuilder()：此方法与你代码的其余部分一起编译，但只有当视图正在准备在 Interface Builder 显示时执行。</li><li>TARGET_INTERFACE_BUILDER：#if TARGET_INTERFACE_BUILDER 预处理宏在 Objective-C 或 Swift 下都是工作的，它会视情况编译正确代码：</li></ul><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#if !TARGET_INTERFACE_BUILDER</span></span><br><span class="line">   <span class="comment">// this code will run in the app itself</span></span><br><span class="line"><span class="meta">#else</span></span><br><span class="line">   <span class="comment">// this code will execute only in IB</span></span><br><span class="line"><span class="meta">#endif</span></span><br></pre></td></tr></table></figure><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="http://nshipster.cn/ibinspectable-ibdesignable/" target="_blank" rel="noopener">IBInspectable / IBDesignable</a><br><a href="https://developer.apple.com/library/ios/recipes/xcode_help-IB_objects_media/Chapters/CreatingaLiveViewofaCustomObject.html" target="_blank" rel="noopener">Creating a Custom View That Renders in Interface Builder</a><br><a href="https://www.weheartswift.com/make-awesome-ui-components-ios-8-using-swift-xcode-6/" target="_blank" rel="noopener">How to make awesome UI components in iOS 8 using Swift and XCode 6</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;当我们拖拽一个 view 到 XIB 上，然后设置 view 属性的时候有没有想到让自己定义的 view 也支持通过 XIB 设置属性，甚至还能实时渲染。&lt;/p&gt;
&lt;p&gt;过去相关接口是不开放的，但是从 XCode6 开始，我们也可以定义像系统控件一样支持在 XIB 视图下设置属性并能实时渲染的控件了。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="UIView" scheme="https://honglu.me/tags/UIView/"/>
    
      <category term="XIB" scheme="https://honglu.me/tags/XIB/"/>
    
  </entry>
  
  <entry>
    <title>通过 XIB 加载 UIView</title>
    <link href="https://honglu.me/2016/02/22/%E9%80%9A%E8%BF%87XIB%E5%8A%A0%E8%BD%BDUIView/"/>
    <id>https://honglu.me/2016/02/22/通过XIB加载UIView/</id>
    <published>2016-02-22T02:13:00.000Z</published>
    <updated>2019-06-12T06:17:51.235Z</updated>
    
    <content type="html"><![CDATA[<p>在项目中有些 view 需要独立做成一个组件，供多个 VC 使用，而且本人项目中习惯使用 XIB，所以这个 view 的组件也希望使用 XIB 来实现。但是当我们创建 UIView 的时候默认是不包含 XIB 文件的，这就需要再创建一个同名的 XIB 文件。但是如何把 XIB 和 UIview 关联起来呢。</p><p>通常我们通过代码加载 XIB 的方法是：</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">+(<span class="keyword">id</span>)viewFromNibNamed:(<span class="built_in">NSString</span>*)nibName owner:(<span class="keyword">id</span>)owner&#123;</span><br><span class="line">    <span class="built_in">NSArray</span>* nibView = [[<span class="built_in">NSBundle</span> mainBundle] loadNibNamed:nibName owner:owner options:<span class="literal">nil</span>];</span><br><span class="line">    <span class="keyword">return</span> [nibView firstObject];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><a id="more"></a><p>注意到上面有一个 owner 属相，这个属性就是表明这个 view 文件的所有者，通常对应一个 VC。</p><p>单纯的创建 view 类和同名的 XIB 是没有效果的，接下来要做的就是把 XIB 和对应的 view 类关联起来</p><p>关联有两种方式，假设自定义的的UIView子类为TestView,三个文件分别为：<code>TestView.h</code>,<code>TestView.m</code>,<code>TestView.xib</code></p><h2 id="XIB-中的-UIView-控件与类关联"><a href="#XIB-中的-UIView-控件与类关联" class="headerlink" title="XIB 中的 UIView 控件与类关联"></a>XIB 中的 UIView 控件与类关联</h2><p><img src="http://blog.qiniu.lololo.store/2016-03-04-通过XIB加载UIView-1.png" alt="通过XIB加载UIView-1"></p><p>这种方式就是设置 XIB 中 view 的 custom class 为TestView。</p><p>使用方式为</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NSArray</span>* nibView = [[<span class="built_in">NSBundle</span> mainBundle] loadNibNamed:<span class="string">@"TestView"</span> owner:<span class="literal">nil</span> options:<span class="literal">nil</span>];</span><br><span class="line">TestView *testView = [nibView firstObject];</span><br><span class="line">[<span class="keyword">self</span>.view addSubview:testView];</span><br></pre></td></tr></table></figure><p>这种方式会通过 <code>initWithCoder:</code> 方法来从 XIB 文件初始化该 view。使用的时候我们可以通过 <code>awakeFromNib</code> 方法来做进一步处理。</p><p>通过这种方式需要注意的是在<code>initWithCoder:</code>方法中，并没有建立 XIB 和 TestView 的 IBOutlet 连接，所以在这里通过代码引用 XIB 中的控件是引用不到的，而当<code>awakeFromNib</code>执行的时候，各种 IBOutlet 都连接好了。所以如果有子控件的初始化工作，最好放在<code>awakeFromNib</code>里面。</p><h2 id="通过-File’s-owner-来关联"><a href="#通过-File’s-owner-来关联" class="headerlink" title="通过 File’s owner 来关联"></a>通过 File’s owner 来关联</h2><p><img src="http://blog.qiniu.lololo.store/2016-03-04-通过XIB加载UIView-2.png" alt="通过XIB加载UIView-2"></p><p>这种方式是设置 XIB 中的 File’s owner 为TestView。</p><p>这种方式创建 view 需要手动来初始化</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">instancetype</span>)initFromNibWithFrame:(<span class="built_in">CGRect</span>)frame owner:(<span class="keyword">id</span>)owner&#123;</span><br><span class="line">    owner = owner ?: <span class="keyword">self</span>;</span><br><span class="line">    <span class="built_in">UIView</span> *nibView = [<span class="built_in">UIView</span> viewFromNibNamed:<span class="built_in">NSStringFromClass</span>([<span class="keyword">self</span> <span class="keyword">class</span>]) owner:owner];</span><br><span class="line">    <span class="keyword">if</span> (!nibView) <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">    nibView.frame = frame;</span><br><span class="line">    <span class="keyword">self</span> = [<span class="keyword">super</span> initWithFrame:nibView.frame];</span><br><span class="line">    [<span class="keyword">self</span> addSubview:nibView];</span><br><span class="line">    [<span class="keyword">self</span> nib_viewDidLoad];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>初始化可以在自定义的<code>nib_viewDidLoad</code>里面进行，这个时候 XIB 和 TestView 的 IBOutlet 连接也已经建立好了</p><p>由于XIB没有设置 view 的 custom class 所以并不会执行<code>initWithCoder:</code>和<code>awakeFromNib</code>方法。</p><h2 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h2><p>上面两种方式都可以实现通过代码加载 XIB 的目的。</p><p>但是有一种情况就是我们的 viewController 也使用 XIB 来布局的，这个时候如果我们拖拽一个 view 在 VC 的 XIB 中，然后设置这个 View 的 custom class 为 TestView 时，上面的方法就无效了。因为虽然同样是 TestView ，但是加载布局文件不同，这里所加载的布局文件是 VC 所在 XIB 的布局文件。</p><p>另外，XIB 中的 file’s owner 其实也就类似是传统 MVC 中的 controller 的角色，因此其最好是 UIVIew,NSObject,UIViewController 这样的类。</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="http://blog.csdn.net/zhangao0086/article/details/45622875" target="_blank" rel="noopener">如何设计一个 iOS 控件?(iOS 控件完全解析)</a><br><a href="http://siroccoicode.club/?p=536" target="_blank" rel="noopener">-loadNibNamed:owner:options:的深入研究</a><br><a href="http://my.oschina.net/mjRao/blog/66214" target="_blank" rel="noopener">ios 开发file’s owner以及outlet与连线的理解</a><br><a href="http://blog.csdn.net/donhao/article/details/7088446" target="_blank" rel="noopener">对xib/nib, file’s owner, first responder的理解</a>  </p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在项目中有些 view 需要独立做成一个组件，供多个 VC 使用，而且本人项目中习惯使用 XIB，所以这个 view 的组件也希望使用 XIB 来实现。但是当我们创建 UIView 的时候默认是不包含 XIB 文件的，这就需要再创建一个同名的 XIB 文件。但是如何把 XIB 和 UIview 关联起来呢。&lt;/p&gt;
&lt;p&gt;通常我们通过代码加载 XIB 的方法是：&lt;/p&gt;
&lt;figure class=&quot;highlight objc&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;+(&lt;span class=&quot;keyword&quot;&gt;id&lt;/span&gt;)viewFromNibNamed:(&lt;span class=&quot;built_in&quot;&gt;NSString&lt;/span&gt;*)nibName owner:(&lt;span class=&quot;keyword&quot;&gt;id&lt;/span&gt;)owner&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;built_in&quot;&gt;NSArray&lt;/span&gt;* nibView = [[&lt;span class=&quot;built_in&quot;&gt;NSBundle&lt;/span&gt; mainBundle] loadNibNamed:nibName owner:owner options:&lt;span class=&quot;literal&quot;&gt;nil&lt;/span&gt;];&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; [nibView firstObject];&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="UIView" scheme="https://honglu.me/tags/UIView/"/>
    
      <category term="XIB" scheme="https://honglu.me/tags/XIB/"/>
    
  </entry>
  
  <entry>
    <title>HTTPS 详解</title>
    <link href="https://honglu.me/2016/01/13/HTTPS%E8%AF%A6%E8%A7%A3/"/>
    <id>https://honglu.me/2016/01/13/HTTPS详解/</id>
    <published>2016-01-13T07:13:00.000Z</published>
    <updated>2019-06-12T06:17:51.235Z</updated>
    
    <content type="html"><![CDATA[<p>移动端的安全问题越来越受到重视，苹果对 iOS 开发已经默认要求使用 HTTPS 了，HTTPS 的安全性毋庸置疑，在移动端使用 HTTPS 通信，可以省去很多安全方面的困扰。这里就简单说一下 HTTPS。</p><a id="more"></a><h1 id="通信过程"><a href="#通信过程" class="headerlink" title="通信过程"></a>通信过程</h1><p><img src="http://blog.qiniu.lololo.store/image%2F16%2F01%2F14%2FHTTPS%E9%80%9A%E4%BF%A1%E8%BF%87%E7%A8%8B.png" alt="HTTPS 通信过程"></p><ol><li>客户端发出握手请求(Client Hello)，包含以下信息：<ul><li>支持的协议版本，比如TLS 1.0版。</li><li>一个客户端生成的随机数(random_1)，这个随机数既需要客户端保存又需要发送给服务器。</li><li>支持的加密方法，比如RSA公钥加密。</li><li>支持的压缩方法。</li></ul></li><li>服务器回复(Server Hello)，包含以下信息：<ul><li>确认使用的加密通信协议版本，比如TLS 1.0版本。如果浏览器与服务器支持的版本不一致，服务器关闭加密通信。</li><li>一个服务器生成的随机数(random_2)。</li><li>确认使用的加密方法，比如RSA公钥加密。</li><li>服务器证书。</li><li>如果服务器需要确认客户端的身份，就会再包含一项请求，要求客户端提供”客户端证书”。比如，金融机构往往只允许认证客户连入自己的网络，就会向正式客户提供USB密钥，里面就包含了一张客户端证书。</li></ul></li><li>客户端回应，包含以下步骤：<ul><li>验证服务器证书的合法性，证书合法性包括：证书是否过期，发行服务器证书的 CA 是否可靠，发行者证书的公钥能否正确解开服务器证书的“发行者的数字签名”，服务器证书上的域名是否和服务器的实际域名相匹配。如果合法性验证没有通过，通讯将断开；</li><li>客户端使用一些加密算法(例如：RSA,Diffie-Hellman)产生一个48个字节的Key，这个Key叫PreMaster Secret。<strong>该PreMaster Secret用服务器公钥加密传送，防止被窃听</strong>。</li><li>编码改变通知，表示随后的信息都将用双方商定的加密方法和密钥发送。</li><li>客户端握手结束通知，表示客户端的握手阶段已经结束。这一项同时也是前面发送的所有内容的hash值，用来供服务器校验。</li><li>如果前一步，服务器要求客户端证书，客户端会在这一步发送证书及相关信息。</li></ul></li><li>服务器回应，服务器通过上面的三个随机数(random_1,random_2,PreMaster Secret)，计算出本次会话的『会话密钥(session secret)』，然后向客户端发送下面信息<ul><li>编码改变通知，表示随后的信息都将用双方商定的加密方法和密钥发送。</li><li>服务器握手结束通知，表示服务器的握手阶段已经结束。这一项同时也是前面发送的所有内容的hash值，用来供客户端校验。</li></ul></li></ol><p>至此，服务器和客户端的握手阶段全部结束，接下来，客户端与服务器进入加密通信，就完全是使用普通的HTTP协议，只不过用『会话密钥(session secret)』对内容做对称加密。</p><h3 id="PreMaster-Secret-说明"><a href="#PreMaster-Secret-说明" class="headerlink" title="PreMaster Secret 说明"></a>PreMaster Secret 说明</h3><p>PreMaster secret是在客户端使用RSA或者Diffie-Hellman等加密算法生成的。它将用来跟服务端和客户端在Hello阶段产生的随机数结合在一起生成Master secret。在客户端使用服务单的公钥对PreMaster secret进行加密之后传送给服务端，服务端将使用私钥进行解密得到PreMaster secret。也就是说服务端和客户端都有一份相同的PreMaster secret和随机数。</p><p>premaster_secret 长度为 48 个字节，前 2 个字节是协议版本号，剩下的 46 个字节填充一个随机数。结构如下：</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Struct &#123;</span><br><span class="line">byte Version[2];</span><br><span class="line">byte random[46];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>PreMaster secret前两个字节是TLS的版本号，这是一个比较重要的用来核对握手数据的版本号，因为在Client Hello阶段，客户端会发送一份加密套件列表和当前支持的SSL/TLS的版本号给服务端，而且是使用明文传送的，如果握手的数据包被破解之后，攻击者很有可能串改数据包，选择一个安全性较低的加密套件和版本给服务端，从而对数据进行破解。所以，服务端需要对密文中解密出来对的PreMaster版本号跟之前Client Hello阶段的版本号进行对比，如果版本号变低，则说明被串改，则立即停止发送任何消息。</p><h3 id="Master-secret-说明"><a href="#Master-secret-说明" class="headerlink" title="Master secret 说明"></a>Master secret 说明</h3><p>需要注意的是，上面握手过程中存在三个Key(random_1,random_2,PreMaster Secret)，而且服务器和客户端都保留着这三个值，客户端和服务端通过这三个值计算出同样的Master secret。Master secret是由系列的hash值组成的，结构如下：</p><p><img src="http://blog.qiniu.lololo.store/image%2F16%2F01%2F14%2FHTTPS%E6%B5%85%E6%9E%90.png" alt="Master secret 结构"></p><p>其中，Client/Server write MAC key 是用来对数据进行验证的，Client/Server write encryption Key 是用来对数据进行加解密的会话密钥(session secret)。</p><p>关于Master Secret的计算请参考<a href="http://www.fenesky.com/blog/2014/07/25/how-session-secret.html" target="_blank" rel="noopener">《Htttps SSL/TLS Session Secret(Key)计算》</a>。</p><p>至于为什么一定要用三个随机数，来生成Master Secret，由于SSL协议中证书是静态的，因此需要引入一种随机因素来保证协商出来的密钥的随机性。SSL协议不信任每个主机都能生成完全随机的随机数，所以这里需要服务器和客户端共生成3个随机数，每增加一个自由度，随机性就会相应增加。  </p><p>同时需要注意前两个随机数都是明文传输的，窃听者是可以轻易获取到的，只有最后一个 PreMaster Secret 是加密传输的，只有拥有服务器私钥才能解密，一旦 PreMaster Secret 泄露，那么本次通信就就完全可被破解了。</p><h3 id="应用数据传输"><a href="#应用数据传输" class="headerlink" title="应用数据传输"></a>应用数据传输</h3><p>在所有的握手阶段都完成之后，就可以开始传送应用数据了。应用数据在传输之前，首先要附加上MAC secret，然后再对这个数据包使用write encryption key进行加密。在服务端收到密文之后，使用Client write encryption key进行解密，客户端收到服务端的数据之后使用Server write encryption key进行解密，然后使用各自的write MAC key对数据的完整性包括是否被串改进行验证。</p><h1 id="对称加密-amp-非对称加密"><a href="#对称加密-amp-非对称加密" class="headerlink" title="对称加密 &amp; 非对称加密"></a>对称加密 &amp; 非对称加密</h1><p>HTTPS 的通信过程中只在握手阶段使用了非对称加密，后面的通信过程均使用的对称加密。尽管非对称加密相比对称加密更加安全，但也存在两个明显缺点：</p><ol><li>CPU 计算资源消耗非常大。一次完全 TLS 握手，密钥交换时的非对称解密计算量占整个握手过程的 90% 以上。而对称加密的计算量只相当于非对称加密的 0.1%，如果应用层数据也使用非对称加解密，性能开销太大，无法承受。</li><li>非对称加密算法对加密内容的长度有限制，不能超过公钥长度。比如现在常用的公钥长度是 2048 位，意味着待加密内容不能超过 256 个字节。</li></ol><p>所以公钥加密目前只能用来作密钥交换或者内容签名，不适合用来做应用层传输内容的加解密。</p><p>非对称密钥交换算法是整个 HTTPS 得以安全的基石，充分理解非对称密钥交换算法是理解 HTTPS 协议和功能的关键。</p><h1 id="证书"><a href="#证书" class="headerlink" title="证书"></a>证书</h1><p>下面是使用 chrome 访问 Google 时查看的证书详细信息</p><p><img src="http://blog.qiniu.lololo.store/image%2F16%2F01%2F14%2FHTTPS%20%E8%AF%81%E4%B9%A6%E7%BB%93%E6%9E%84.png" alt="Google  HTTPS证书"></p><p>数字证书的格式普遍采用的是X.509V3国际标准，一个标准的X.509数字证书包含以下一些内容：</p><ul><li>证书的版本信息；</li><li>证书的序列号，每个证书都有一个唯一的证书序列号；</li><li>证书所使用的签名算法；</li><li>证书的发行机构名称，命名规则一般采用X.500格式；</li><li>证书的有效期，通用的证书一般采用UTC时间格式，它的计时范围为1950-2049；</li><li>证书所有人的名称，命名规则一般采用X.500格式；</li><li>证书所有人的公钥；</li><li>证书发行者对证书的签名。</li></ul><p>证书以链的形式组织，上级标识该证书的签发机构，验证证书的时候也是顺着这个链向上层层验证的，只有所有证书都是受信的，整个验证结果才是可信的。那么根证书是如何验证的呢？根证书是自信任的，在操作系统或者浏览其中都会默认一些受信任的 CA 机构根证书。</p><p>除非对这个根证书有绝对的信任才可以加入信任列表中，因为根证书是有权签发子证书的，如果根证书失信，那么对应的子证书的可信性就无从谈起，那么与使用相应证书的 HTTPS 网站通信的安全性就得不到保障了。当访问12306的时候，网站会提示把其提供的根证书加入到可信任列表里面，这就是为什么许多人反对这种做法的原因了。另外中国互联网信息中心(CNNIC)也曾发布过用于中间人攻击证书，详情可以参考这篇文章：<a href="http://www.williamlong.info/archives/4183.html" target="_blank" rel="noopener">谷歌称CNNIC发布伪造CA证书</a>。</p><p>Mac 用户可以通过 钥匙串-&gt;系统根证书 查看系统默认信任的所有根证书，如果想取消信任(比如 CNNIC证书)，可以双击，改为永不信任</p><h1 id="安全性"><a href="#安全性" class="headerlink" title="安全性"></a>安全性</h1><p>针对 HTTPS 的攻击最主要的就是 SSL 劫持攻击，其分为两种：</p><h3 id="HTTPS-替换为-HTTP"><a href="#HTTPS-替换为-HTTP" class="headerlink" title="HTTPS 替换为 HTTP"></a>HTTPS 替换为 HTTP</h3><p>这种方式就是攻击者充当中间人和服务器通信，然后把相应的通信内容通过 HTTP 协议发送给客户端，由于 HTTP 协议是未加密的，于是就可以截获用户的访问数据。</p><p>这种攻击方式比较简单，通过代理，可以很容易的把 HTTPS 变成 HTTP，这个一方面需要用户留意网站是否有从 HTTPS 跳转到 HTTP 的行为，另一方面服务器也可以通过配置将所有HTTP的请求强制转移到HTTPS上。</p><h3 id="HTTPS-劫持"><a href="#HTTPS-劫持" class="headerlink" title="HTTPS 劫持"></a>HTTPS 劫持</h3><p>这种方式攻击者为了获得 HTTPS 的明文传输内容，需要充当中间人，替换服务器发给用户的包含公钥的证书。攻击者既和用户之间建立了 HTTPS 链接，又和服务器建立了 HTTPS 链接。</p><p>在上面握手建立的过程中，由于用户的公钥是攻击者生成的，所以攻击者可以轻易获得握手中的数据。也就可以获取到和用户通信过程中的对称加密的密钥，攻击者可以通过密钥获取用户发送的数据，同时在使用和服务器通信的密钥加密后再发送给服务器。</p><p>这种攻击方式也有一个明显的问题就是攻击者生成的证书几乎是不可能被用户信任的，在这种情况下，用户浏览器通常会提示该网站的证书不可信，是否继续访问，这已经对用户进行了一个明显的警告了。</p><p>另外我们也可以通过这种对基于 HTTPS 的通信进行抓包分析。Mac 平台著名的抓包工具 Charles  就是基于这种方式，首先要求你信任一个它的证书，然后自己充当中间人对你与某个服务器的 HTTPS 通信进行抓包分析。</p><h3 id="其它"><a href="#其它" class="headerlink" title="其它"></a>其它</h3><p>除了上述两种攻击方式之外，就是由于程序漏洞造成的问题了，比如早期 HTTPS 曾被发现存在漏洞，但很快被修补了；再比如2014年 OpenSSL 爆出的『心脏出血』漏洞；再比如2015年爆出iOS 平台 AFNetworking 存在漏洞，导致上千个 APP 存在安全性风险。但是类似的问题相对来说还是比较少见的。</p><p>总体来说，HTTPS 本身是比较安全的，存在的问题用户也都是可觉察的，只要用户有相应的安全意识，涉及个人重要信息的网站使用 HTTPS 访问，同时注意网站证书是否可信，基本不会出现被劫持的情况。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="http://www.fenesky.com/blog/2014/07/19/how-https-works.html" target="_blank" rel="noopener">Https(SSL/TLS)原理详解</a><br><a href="http://www.fenesky.com/blog/2014/07/25/how-session-secret.html" target="_blank" rel="noopener">Htttps SSL/TLS Session Secret(Key)计算</a><br><a href="http://haomou.net/2014/08/30/2014_https/" target="_blank" rel="noopener">HTTPS详解 SSL/TLS</a><br><a href="https://www.evssl.cn/ev-ssl-news2/https1.html" target="_blank" rel="noopener">大型互联网站点HTTPS实践（一）：详解HTTPS协议和原理</a><br><a href="http://www.cnblogs.com/hyddd/archive/2009/01/07/1371292.html" target="_blank" rel="noopener">浅析数字证书</a><br><a href="http://www.guokr.com/post/114121/" target="_blank" rel="noopener">HTTPS那些事（一）HTTPS原理</a><br><a href="http://www.guokr.com/post/116169/" target="_blank" rel="noopener">HTTPS那些事（二）SSL证书</a><br><a href="http://www.guokr.com/blog/148613/" target="_blank" rel="noopener">HTTPS那些事（三）攻击实例与防御</a><br><a href="https://www.zhihu.com/question/22779469" target="_blank" rel="noopener">使用 HTTPS 的网站也能被黑客监听到数据吗？</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;移动端的安全问题越来越受到重视，苹果对 iOS 开发已经默认要求使用 HTTPS 了，HTTPS 的安全性毋庸置疑，在移动端使用 HTTPS 通信，可以省去很多安全方面的困扰。这里就简单说一下 HTTPS。&lt;/p&gt;
    
    </summary>
    
      <category term="HTTP" scheme="https://honglu.me/categories/HTTP/"/>
    
    
      <category term="HTTPS" scheme="https://honglu.me/tags/HTTPS/"/>
    
  </entry>
  
  <entry>
    <title>AFNetworking 源码阅读</title>
    <link href="https://honglu.me/2016/01/11/AFNetworking%20%E6%BA%90%E7%A0%81%E9%98%85%E8%AF%BB/"/>
    <id>https://honglu.me/2016/01/11/AFNetworking 源码阅读/</id>
    <published>2016-01-11T09:17:00.000Z</published>
    <updated>2018-08-09T03:55:11.779Z</updated>
    
    <content type="html"><![CDATA[<p>AFNetworking 3.0 发布了，这个版本去除了 NSURLConnectionOperation，使用 NSURLSession 代替，于是，这个版本只支持 iOS 7以及以上版本，代码也相应的精简了不少，于是利用一周时间阅读了一下 AFNetworking 3.0 的源码。</p><a id="more"></a><h2 id="技巧"><a href="#技巧" class="headerlink" title="技巧"></a>技巧</h2><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="meta">#pragma clang diagnostic push</span></span><br><span class="line"><span class="meta">#pragma clang diagnostic ignored <span class="meta-string">"-Wgnu"</span></span></span><br><span class="line"><span class="comment">//code</span></span><br><span class="line"><span class="meta">#pragma clang diagnostic pop</span></span><br></pre></td></tr></table></figure><p>使用 <code>:?</code> 符号，忽略编译器的警告</p><h2 id="iOS-bug-fix"><a href="#iOS-bug-fix" class="headerlink" title="iOS bug fix"></a>iOS bug fix</h2><p>AFURLSessionManager.m<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 解决NSURLSessionTask 的 taskIdentifier 在并发的情况下不是唯一的 bug，苹果在 iOS8 的时候解决了这个 bug</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> url_session_manager_create_task_safely(dispatch_block_t block) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">NSFoundationVersionNumber</span> &lt; <span class="built_in">NSFoundationVersionNumber_With_Fixed_5871104061079552_bug</span>) &#123;</span><br><span class="line">        <span class="comment">// Fix of bug</span></span><br><span class="line">        <span class="comment">// Open Radar:http://openradar.appspot.com/radar?id=5871104061079552 (status: Fixed in iOS8)</span></span><br><span class="line">        <span class="comment">// Issue about:https://github.com/AFNetworking/AFNetworking/issues/2093</span></span><br><span class="line">        <span class="built_in">dispatch_sync</span>(url_session_manager_creation_queue(), block);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        block();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>AFURLSessionManager.h<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Whether to attempt to retry creation of upload tasks for background sessions when initial call returns `nil`. `NO` by default.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> @bug As of iOS 7.0, there is a bug where upload tasks created for background tasks are sometimes `nil`. As a workaround, if this property is `YES`, AFNetworking will follow Apple's recommendation to try creating the task again.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> @see https://github.com/AFNetworking/AFNetworking/issues/1675</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">// iOS7.0 的一个 bug，当创建 后台的 upload task 的时候可能是 nil，这个值为 YES 的时候会尝试重新创建</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">BOOL</span> attemptsToRecreateUploadTasksForBackgroundSessions;</span><br></pre></td></tr></table></figure></p><h2 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h2><h3 id="dispatch-semaphore的使用"><a href="#dispatch-semaphore的使用" class="headerlink" title="dispatch_semaphore的使用"></a>dispatch_semaphore的使用</h3><p>在 <code>AFURLSessionManager.m</code> 里面的 <code>tasksForKeyPath:</code> 方法，通过引入信号量的方式把<code>NSURLSession</code>的异步方法 <code>getTasksWithCompletionHandler:</code> 变成了同步方法</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="built_in">NSArray</span> *)tasksForKeyPath:(<span class="built_in">NSString</span> *)keyPath &#123;</span><br><span class="line">    __block <span class="built_in">NSArray</span> *tasks = <span class="literal">nil</span>;</span><br><span class="line">    <span class="comment">// 这里是把本来异步的getTasksWithCompletionHandler方法变成了同步的方式了，通过引入信号量的方式，等待异步方法获取到tasks，然后再返回</span></span><br><span class="line">    dispatch_semaphore_t semaphore = dispatch_semaphore_create(<span class="number">0</span>);</span><br><span class="line">    [<span class="keyword">self</span>.session getTasksWithCompletionHandler:^(<span class="built_in">NSArray</span> *dataTasks, <span class="built_in">NSArray</span> *uploadTasks, <span class="built_in">NSArray</span> *downloadTasks) &#123;</span><br><span class="line">        <span class="keyword">if</span> ([keyPath isEqualToString:<span class="built_in">NSStringFromSelector</span>(<span class="keyword">@selector</span>(dataTasks))]) &#123;</span><br><span class="line">            tasks = dataTasks;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> ([keyPath isEqualToString:<span class="built_in">NSStringFromSelector</span>(<span class="keyword">@selector</span>(uploadTasks))]) &#123;</span><br><span class="line">            tasks = uploadTasks;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> ([keyPath isEqualToString:<span class="built_in">NSStringFromSelector</span>(<span class="keyword">@selector</span>(downloadTasks))]) &#123;</span><br><span class="line">            tasks = downloadTasks;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> ([keyPath isEqualToString:<span class="built_in">NSStringFromSelector</span>(<span class="keyword">@selector</span>(tasks))]) &#123;</span><br><span class="line">            tasks = [@[dataTasks, uploadTasks, downloadTasks] valueForKeyPath:<span class="string">@"@unionOfArrays.self"</span>];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        dispatch_semaphore_signal(semaphore);</span><br><span class="line">    &#125;];</span><br><span class="line"></span><br><span class="line">    dispatch_semaphore_wait(semaphore, DISPATCH_TIME_FOREVER);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> tasks;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面代码中 <a href="mailto:`@unionOfArrays.self" target="_blank" rel="noopener">`@unionOfArrays.self</a>` 参考：<a href="http://nshipster.com/kvc-collection-operators/" target="_blank" rel="noopener">http://nshipster.com/kvc-collection-operators/</a></p><h2 id="作者声明二级指针"><a href="#作者声明二级指针" class="headerlink" title="作者声明二级指针"></a>作者声明二级指针</h2><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这是一个指向指针的指针，二级指针</span></span><br><span class="line"><span class="comment">// https://www.zhihu.com/question/35661618</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> *AFHTTPRequestSerializerObserverContext = &amp;AFHTTPRequestSerializerObserverContext;</span><br></pre></td></tr></table></figure><p>这里是为了做 KVO 的时候给 context 赋值，便于区分不同的 context，由于这个AFHTTPRequestSerializerObserverContext的内存地址是在编译的时候就决定了，所以这么声明确实可以保证 context 不同，是个比较讨巧的方式</p><h2 id="static-方法"><a href="#static-方法" class="headerlink" title="static 方法"></a>static 方法</h2><p>作者在多处使用 static 方法</p><h2 id="参考-amp-代码地址"><a href="#参考-amp-代码地址" class="headerlink" title="参考 &amp; 代码地址"></a>参考 &amp; 代码地址</h2><p>边阅读边标注，阅读标注后的代码地址：<a href="https://github.com/pljhonglu/AFNetworking_Lew" target="_blank" rel="noopener">AFNetworking 3.0</a></p><p>参考：</p><ul><li><a href="http://blog.cnbang.net/tech/2320/" target="_blank" rel="noopener">AFNetworking2.0源码解析&lt;一&gt;</a></li><li><a href="http://blog.cnbang.net/tech/2371/" target="_blank" rel="noopener">AFNetworking2.0源码解析&lt;二&gt;</a></li><li><a href="http://blog.cnbang.net/tech/2416/" target="_blank" rel="noopener">AFNetworking2.0源码解析&lt;三&gt;</a></li><li><a href="http://blog.cnbang.net/tech/2456/" target="_blank" rel="noopener">AFNetworking2.0源码解析&lt;四&gt;</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;AFNetworking 3.0 发布了，这个版本去除了 NSURLConnectionOperation，使用 NSURLSession 代替，于是，这个版本只支持 iOS 7以及以上版本，代码也相应的精简了不少，于是利用一周时间阅读了一下 AFNetworking 3.0 的源码。&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="AFNetworking" scheme="https://honglu.me/tags/AFNetworking/"/>
    
  </entry>
  
  <entry>
    <title>使用xib加载View的小技巧</title>
    <link href="https://honglu.me/2015/11/19/%E4%BD%BF%E7%94%A8xib%E5%8A%A0%E8%BD%BDView%E7%9A%84%E5%B0%8F%E6%8A%80%E5%B7%A7/"/>
    <id>https://honglu.me/2015/11/19/使用xib加载View的小技巧/</id>
    <published>2015-11-19T10:24:39.000Z</published>
    <updated>2018-08-09T03:55:11.820Z</updated>
    
    <content type="html"><![CDATA[<p>本人在开发中比较喜欢用 xib 来作布局，通常情况下，我们需要把一些常用的 view 封装成组件来供大伙儿使用。但是这个组件一般只是负责 view 层的展示任务，事件相关的业务逻辑需要转发到相应的 controller 里面去。这里介绍一下本人使用的一种方法。</p><p>直接上代码</p><a id="more"></a><h2 id="BaseView-基类"><a href="#BaseView-基类" class="headerlink" title="BaseView 基类"></a>BaseView 基类</h2><p>BaseView<br><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">BaseView</span> : <span class="title">UIView</span></span></span><br><span class="line">+ (<span class="keyword">instancetype</span>)loadViewFromNib;</span><br><span class="line">+ (<span class="keyword">instancetype</span>)loadViewFromNibWithOwner:(<span class="keyword">id</span>)owner;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">BaseView</span></span></span><br><span class="line">+ (<span class="keyword">instancetype</span>)loadViewFromNib&#123;</span><br><span class="line">    BaseView* view = [[<span class="keyword">self</span> alloc]init];</span><br><span class="line">    <span class="built_in">UIView</span>* nibView = [<span class="keyword">self</span> viewFromNibNamed:<span class="built_in">NSStringFromClass</span>([<span class="keyword">self</span> <span class="keyword">class</span>]) owner:view];</span><br><span class="line">    view.frame = nibView.frame;</span><br><span class="line">    [view addSubview:nibView];</span><br><span class="line">    <span class="keyword">return</span> view;</span><br><span class="line">&#125;</span><br><span class="line">+(<span class="keyword">instancetype</span>)loadViewFromNibWithOwner:(<span class="keyword">id</span>)owner&#123;</span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">self</span> viewFromNibNamed:<span class="built_in">NSStringFromClass</span>([<span class="keyword">self</span> <span class="keyword">class</span>]) owner:owner];</span><br><span class="line">&#125;</span><br><span class="line">+(<span class="keyword">instancetype</span>)viewFromNibNamed:(<span class="built_in">NSString</span>*)nibName owner:(<span class="keyword">id</span>)owner&#123;</span><br><span class="line">    <span class="built_in">NSArray</span>* nibView = [[<span class="built_in">NSBundle</span> mainBundle] loadNibNamed:nibName owner:owner options:<span class="literal">nil</span>];</span><br><span class="line">    <span class="keyword">return</span> [nibView firstObject];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><h2 id="MyView-组件"><a href="#MyView-组件" class="headerlink" title="MyView 组件"></a>MyView 组件</h2><p>首先建一个 BaseView 的子类，和对应的 xib 文件</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">MyView.h</span><br><span class="line">MyView.m</span><br><span class="line">MyView.xib</span><br></pre></td></tr></table></figure><p>MyView.h</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@protocol</span> <span class="title">MyViewActionProtocol</span> &lt;<span class="title">NSObject</span>&gt;</span></span><br><span class="line">- (<span class="keyword">IBAction</span>)loginAction:(<span class="keyword">id</span>)sender;</span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">MyView</span> : <span class="title">BaseView</span>&lt;<span class="title">MyViewActionProtocol</span>&gt;</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">weak</span>)<span class="keyword">IBOutlet</span> <span class="built_in">UIButton</span>* loginButton;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure><p>在 MyView.xib 中，把 file’s owner 设置为 MyView，同时把 view 的 custom class 设置为 MyView。</p><p>接下来就是做关联了：</p><ul><li>关联属性的时候需要把 xib 中的 button 与 <code>view</code> 的 loginButton 关联</li><li>关联事件的时候需要把 xib 中的 button 的事件与 <code>File&#39;s Owner</code> 的 loginAction 关联</li></ul><p>这样就做好了一个 MyView 组件了</p><h2 id="使用-MyView"><a href="#使用-MyView" class="headerlink" title="使用 MyView"></a>使用 MyView</h2><p>在 MyViewController 中我们要加入这个 MyView，可以通过如下方式</p><p>首先，让 MyViewController 实现 MyViewActionProtocol 协议</p><p>然后在 viewDidLoad 的时候如下方式加入 MyView</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">MyView* view = [MyView loadViewFromNibWithOwner:<span class="keyword">self</span>];</span><br><span class="line">[<span class="keyword">self</span>.view addSubview:view];</span><br></pre></td></tr></table></figure><p>这样，view 中的事件处理就可以直接放到 MyViewController 里面去了，但是 view 的渲染我们照样可以在 MyView 中进行。<br>比如，我要设置 loginButton 的样式，可以在 MyView 里面写如下代码</p><figure class="highlight objc"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)layoutSubviews&#123;</span><br><span class="line">    [_loginButton setBackgroundColor:[<span class="built_in">UIColor</span> redColor]];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而且在 MyViewController 中照样可以引用到 MyView 里面的属性</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="http://www.ifun.cc/blog/2014/02/22/ioskai-fa-zhi-xibji-qiao-jie-shao/" target="_blank" rel="noopener">iOS开发之xib技巧介绍</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本人在开发中比较喜欢用 xib 来作布局，通常情况下，我们需要把一些常用的 view 封装成组件来供大伙儿使用。但是这个组件一般只是负责 view 层的展示任务，事件相关的业务逻辑需要转发到相应的 controller 里面去。这里介绍一下本人使用的一种方法。&lt;/p&gt;
&lt;p&gt;直接上代码&lt;/p&gt;
    
    </summary>
    
      <category term="iOS" scheme="https://honglu.me/categories/iOS/"/>
    
    
      <category term="iOS" scheme="https://honglu.me/tags/iOS/"/>
    
      <category term="UIView" scheme="https://honglu.me/tags/UIView/"/>
    
      <category term="xib" scheme="https://honglu.me/tags/xib/"/>
    
  </entry>
  
  <entry>
    <title>给 iTerm 终端设置代理</title>
    <link href="https://honglu.me/2015/11/06/%E7%BB%99iTerm%E7%BB%88%E7%AB%AF%E8%AE%BE%E7%BD%AE%E4%BB%A3%E7%90%86/"/>
    <id>https://honglu.me/2015/11/06/给iTerm终端设置代理/</id>
    <published>2015-11-06T05:31:19.000Z</published>
    <updated>2018-08-09T03:55:11.827Z</updated>
    
    <content type="html"><![CDATA[<h4 id="1-设置代理"><a href="#1-设置代理" class="headerlink" title="1.设置代理"></a>1.设置代理</h4><p>使用 curl，wget，brew等http应用程序会调用http_proxy和https_proxy这两环境变量进行代理，通过下面方式设置：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> http_proxy=http://127.0.0.1:8087</span><br><span class="line"><span class="built_in">export</span> https_proxy=<span class="variable">$http_proxy</span></span><br></pre></td></tr></table></figure><a id="more"></a><h4 id="2-取消设置"><a href="#2-取消设置" class="headerlink" title="2.取消设置"></a>2.取消设置</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">unset</span> http_proxy https_proxy</span><br></pre></td></tr></table></figure><h4 id="3-快速切换"><a href="#3-快速切换" class="headerlink" title="3.快速切换"></a>3.快速切换</h4><p>可以在 <code>~/.zshrc</code> 或者 <code>~/.bash_profile</code> 中添加这样的alias：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> goproxy=<span class="string">'export http_proxy=http://127.0.0.1:8087 https_proxy=http://127.0.0.1:8087'</span></span><br><span class="line"><span class="built_in">alias</span> disproxy=<span class="string">'unset http_proxy https_proxy'</span></span><br></pre></td></tr></table></figure><p>这样下次就可以很方便地切换proxy了！</p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="http://hushicai.com/2014/04/08/mac-osx-ming-ling-xing-xia-li-yong-goagent-zuo-dai-li.html" target="_blank" rel="noopener">mac osx命令行下利用goagent作代理</a><br><a href="https://fengqi.me/unix/328.html" target="_blank" rel="noopener">利用 Privoxy 让命令行下的 wget 和 curl 等命令实现自动代理</a></p>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;1-设置代理&quot;&gt;&lt;a href=&quot;#1-设置代理&quot; class=&quot;headerlink&quot; title=&quot;1.设置代理&quot;&gt;&lt;/a&gt;1.设置代理&lt;/h4&gt;&lt;p&gt;使用 curl，wget，brew等http应用程序会调用http_proxy和https_proxy这两环境变量进行代理，通过下面方式设置：&lt;/p&gt;
&lt;figure class=&quot;highlight bash&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;built_in&quot;&gt;export&lt;/span&gt; http_proxy=http://127.0.0.1:8087&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;built_in&quot;&gt;export&lt;/span&gt; https_proxy=&lt;span class=&quot;variable&quot;&gt;$http_proxy&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="工具党" scheme="https://honglu.me/categories/%E5%B7%A5%E5%85%B7%E5%85%9A/"/>
    
    
      <category term="proxy" scheme="https://honglu.me/tags/proxy/"/>
    
      <category term="iTerm" scheme="https://honglu.me/tags/iTerm/"/>
    
  </entry>
  
</feed>
